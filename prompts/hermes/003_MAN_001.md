# Hermes prompt — MAN-001

Implement only `tickets/MAN-001.md` after CAT-001 and RAW-001 are accepted. Use canonical JSON and immutable manifests. Dataset identity must derive from declared lineage and output content; a path or timestamp alone is never identity. Do not implement source normalizers or factors. Stop after the ticket.
