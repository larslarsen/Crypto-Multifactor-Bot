# Hermes prompt — CAT-001

Implement **only** `tickets/CAT-001.md` in the current repository.

Before changing code, read the research charter, architecture docs, ADRs, coding standards, and layer matrix. Inspect the existing repository and reuse compatible scaffold code rather than duplicating it.

Constraints:

- Python 3.12;
- standard `sqlite3` unless an existing approved dependency already provides migrations;
- no Alembic, SQLAlchemy, server database, daemon, or background scheduler;
- forward-only numbered `.sql` migrations;
- migrations and their SHA-256 hashes recorded transactionally;
- foreign keys enabled per connection;
- bounded busy timeout and local WAL mode;
- exact, actionable errors;
- tests must use temporary directories and databases;
- do not implement RAW-001 or Evidence Registry behavior in this ticket, though the migration runner must later be able to apply migration 0002.

Required response after implementation:

1. files changed;
2. design choices;
3. commands executed and exact results;
4. acceptance criteria demonstrated;
5. unresolved risks;
6. proposed commit message.

Stop after CAT-001.
