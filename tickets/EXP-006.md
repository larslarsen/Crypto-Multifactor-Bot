# EXP-006 — Multi-Fold OOS of Frozen TSMOM Configs on Extended History

**Priority:** P0  
**Status:** READY  
**Dependencies:** DATA-004 (ACCEPTED), ALLOC-001, EXP-005  
**Layer:** research / factors / paper evidence  
**Architecture:** ALLOC-001 risk; real as-of on DATA-004 extended `market_bars`. **No LIVE.**

## Objective

DATA-004 provides ~30.7 months of real daily bars (2024-01-01 → 2026-07-23). Re-validate frozen TSMOM configs with a **credible multi-fold or long holdout** protocol (not the thin 8+7 week EXP-005 split).

## Scope

1. **Dataset:** Pin DATA-004 canonical id  
   `ds_a17651d5c871656f18c29d50fe96d41fa9f08eee8436b276237f96a679764dcd`  
   (or `resolve_latest` + record id). Document `quality_status: REJECTED` / 1d native caveat from REVIEW-0195.
2. **Configs (frozen — no re-grid on test folds):**
   - EXP-004 full-window notables: `tsmom_14_0`, `tsmom_14_3`, `tsmom_7_0`
   - EXP-005 train picks if different: `tsmom_60_0`
   - Baseline: `tsmom_30_7`
3. **Protocol (required):** Walk-forward **or** ≥2 sequential holdout folds with documented dates. Allow ≥90d warmup before first decision when lookback=90 is in set (baseline is 30; still leave warm start from bar_start). Prefer weekly decisions.
4. **Risk:** ALLOC-001 neutrality-preserving 0.15 / 1.0.
5. **Artifact** `research/sprint_004/21_TSMOM_EXTENDED_OOS.json`:
   - protocol, fold dates, per-config per-fold metrics (net return, max |w|, gross, max |net|, meets_risk, live_gate_satisfied)
   - `live_eligible: false` globally and per row
   - `oos_supports_live_path: false` unless **out-of-sample** folds show gate true; still no LIVE ticket from this work
6. **Do not mutate** artifacts 08–20.
7. **Tests:** suite green.

## Out of Scope

- LIVE / promotion of new model artifacts  
- Expanding lookback/skip grid on test folds  
- Changing risk limits  
- Fixing BAR-001 daily quality (optional note only)

## Acceptance (Jr)

1. `.venv/bin/python -m pytest tests/execution/ tests/acquisition/ -q --tb=short`
2. `.venv/bin/python -m ruff check src/cryptofactors/execution scripts/`
3. `21_TSMOM_EXTENDED_OOS.json` present; all `live_eligible: false`
4. `python3 scripts/check_repo_control.py`

## Stop Condition

After Sr: AWAITING_REVIEW, Next ticket authorized: NONE.
