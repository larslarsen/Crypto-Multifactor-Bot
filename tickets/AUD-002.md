# AUD-002 — Complete the reusable source-audit toolkit

**Status:** IN_PROGRESS

**Priority:** P0
**Dependencies:** GOV-001 (accepted); builds on the provisional AUD-001-labelled toolkit commits
**Layer:** quality / source-audit

> NOTE: This ticket is distinct from `AUD-001` (Schema and coverage profiler). Do not reuse or
> rewrite `AUD-001`; that ticket describes a different deliverable with different dependencies.

## Objective

Complete the reusable `src/source_audit/` toolkit and its focused test code so it is
reviewable, deterministic, and ready for senior review.

## Scope (this ticket only)

- `src/source_audit/`
- source-audit-focused test code
- atomic content-addressed downloads
- safe archive and CSV inspection
- timestamp inference
- Binance precision comparison
- robust pagination
- trade-to-bar reconstruction and candle comparison
- storage statistics
- deterministic serialization

## Explicitly out of scope

- Any real source data, research conclusions, architecture changes, or provider acceptance
  decisions. None of these belong in this ticket.
- `AUD-001` (schema/coverage profiler) is not rewritten or superseded by this ticket.

## Provisional history notice

Existing commits through `247787b` that carried the AUD-001 label for this toolkit work are
**provisional and incomplete**. They are governed prospectively by this ticket
(`CHANGES_REQUIRED` review on record; see `docs/reviews/REVIEW-0005_AUD-001_TOOLKIT_CHANGES_REQUIRED.md`).
Passing unrelated repository tests does **not** constitute acceptance of this toolkit.

## Authorized scope

Complete the reusable source-audit toolkit and focused test code only.

## Required outcome

AUD-002 implementation is ready for senior review.

## Acceptance gate

- `scripts/check_repo_control.py` passes on `main`.
- `src/source_audit/` and the source-audit-focused tests are coherent and deterministic.
- No real source data, research conclusions, architecture changes, or provider acceptance
  decisions are introduced.

## Stop condition

Commit the implementation handoff state and stop.

## Status governance

Only the owner or a designated reviewer may mark this ticket `ACCEPTED`.
