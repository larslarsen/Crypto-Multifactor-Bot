# EXP-003 — Risk-Compliant MOM-TS Real-Data Diagnosis

**Priority:** P0  
**Status:** ACCEPTED  
**Dependencies:** PAPER-006 (ACCEPTED), DATA-003, EXP-002  
**Layer:** research / experiments / execution evidence  
**Architecture:** use existing risk-enforced paper path + catalog bars. **No LIVE.**

## Objective

Explain and extend the PAPER-006 result: risk-compliant MOM-TS-01 is **−3.31%** on real as-of (Apr–May 2026) while unconstrained was **+1.37%** with illegal concentration. Produce a decision-grade diagnosis and longer-window evidence under the same risk limits.

## Scope

1. **Diagnosis artifact** `research/sprint_004/15_MOMTS_RISK_DIAGNOSIS.json` (and short markdown optional under research/ only if needed for tables):
   - Compare unconstrained vs enforced weights (max weight, gross, net exposure) on the same decision dates
   - Attribute return gap to clipping / gross scale / turnover
   - Note dollar-neutrality drift after clip-and-renormalize

2. **Longer real window** (if Binance history allows): backfill + risk-enforced paper from earliest available through latest for the 10-name mapped universe (or document data start constraints). Target ≥6 months of weekly decisions if available.

3. **Experiment registration** — register/update experiment row in existing experiment CSV/registry pattern used by EXP-002; mark EXECUTED with pointers to artifacts.

4. **Gate fields** — any session summary must use `compute_live_gate_satisfied`; `live_eligible: false` always.

5. **Tests** — only if new pure functions added; otherwise reuse suite. No LIVE tests.

## Out of Scope

- LIVE promotion  
- New factor families (stay on tsmom_30_7 unless diagnosis proves lookback change is required — then stop and request new ticket)  
- Changing risk limits (0.15 / 1.0 fixed)

## Acceptance (Jr)

1. `.venv/bin/python -m pytest tests/execution/ tests/acquisition/ -q --tb=short`
2. `.venv/bin/python -m ruff check src/cryptofactors/execution scripts/`
3. Artifacts `15_MOMTS_RISK_DIAGNOSIS.json` (+ longer session JSON if produced) exist; `live_eligible: false`
4. `python3 scripts/check_repo_control.py`

## Stop Condition

After Sr: AWAITING_REVIEW, Next ticket authorized: NONE.
