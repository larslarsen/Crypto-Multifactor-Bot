# DEX-003 - Data-First Uniswap V2 Survivorship Substrate

**Priority:** P0
**Status:** IN_PROGRESS
**Next required actor:** Jr Dev - Hermes - publish Sol source rejection and twenty-first-correction authorization
**Dependencies:** DEX-002 (ACCEPTED), DATA-012 PairCreated ingestion substrate, ADR-0015
**Layer:** acquisition / canonical data / point-in-time universe
**Architecture:** `docs/adr/0015-data-first-dex-research-substrate.md`

## Objective

Build the first research-authoritative DEX dataset before any DEX factor or experiment:
a complete Ethereum Uniswap V2 factory census, complete historical Swap/Sync event
coverage for every direct USDC/USDT pair, canonical daily pool observations, and
point-in-time active/inactive membership without survivorship filtering.

## Required Domain

- Ethereum mainnet only.
- Canonical Uniswap V2 factory only.
- Every PairCreated event from deployment block 10,000,835 through a pinned finalized
  cutoff.
- Every pair where exactly one token is an accepted USDC or USDT quote.
- Current web-API visibility or liquidity must not determine inclusion.

## Scope

1. Extend the existing raw JSON-RPC acquisition pattern to Pair `Swap` and `Sync`
   events, block headers, and token-decimal calls.
2. Preserve requests, responses, receipts, block identities, and raw dependencies.
3. Prove Swap/Sync completeness through two independent RPC providers per block chunk
   or canonical receipt-level reconstruction. One successful `eth_getLogs` response is
   insufficient.
4. Publish the four immutable ADR-0015 products: `dex_pool_registry`,
   `dex_pool_events`, `dex_pool_daily`, and `dex_universe_daily`.
5. Freeze base/stable-quote orientation and retain event time, conservative
   `source_available_at`, and actual `retrieved_at` separately.
6. Derive daily `ACTIVE`, `INACTIVE`, `UNAVAILABLE`, and `REVIVED` states using only
   lagged historical observations. Never delete a pool or infer inactivity from missing
   acquisition evidence.
7. Seal calendar year 2025 as structural holdout data. Quality checks may inspect
   schema, hashes, counts, and gaps, but no factor, return, ranking, or portfolio output.
8. Publish `research/sprint_004/51_DEX_SURVIVORSHIP.json` with census counts, block
   coverage, source reconciliation, event coverage by pool, active/inactive/revived
   histories, unresolved gaps, dataset IDs, and catalog reconciliation.

## Blocking Gates

1. PairCreated coverage is contiguous and reconciles the complete factory census.
2. Every selected pair has complete, reconciled Swap/Sync coverage from creation to
   cutoff; any unresolved range blocks PASS.
3. Token identity, decimals, orientation, OHLC, volume, reserves, and liquidity
   reconstruct deterministically from retained evidence.
4. Known inactive and revived pools reconstruct correctly at historical dates.
5. Decisions at time `t` cannot observe events unavailable at `t` or same-day closes.
6. Current API snapshots do not control historical membership or canonical prices.

## Out of Scope

- Factor design, parameter selection, backtests, or experiment execution.
- Other DEX protocols or chains.
- GeckoTerminal, DexScreener, DefiLlama, or Birdeye as historical authority.
- DEX execution costs, PAPER, promotion, or LIVE.

## Acceptance (Jr)

1. Ticket-focused tests cover raw lineage, range reconciliation, provider disagreement,
   reorg/finality handling, orientation, daily reconstruction, point-in-time membership,
   inactivity/revival, and holdout access blocking.
2. `.venv/bin/python -m pytest tests/ -q --tb=short`
3. `.venv/bin/python -m ruff check src/cryptofactors/ scripts/`
4. The report exists and every blocking gate is PASS with no unresolved event range.
5. `python3 scripts/check_repo_control.py`

## Implementation Sequence

1. PairCreated source, complete census, and publication: COMPLETE.
2. Swap/Sync ingestion source and focused tests: COMPLETE; production execution pending.
3. Deterministic pool-registry source and focused tests: COMPLETE.
4. Corrected registry publication and supersession: COMPLETE.
5. Sr Dev production event-acquisition orchestration and correction pass: COMPLETE.
6. Jr Dev integration, migration 0016, and aligned dual-provider pilot: COMPLETE.
7. Scalar production acquisition paused after 2,891 retained v1 receipts: COMPLETE.
8. Max architecture amendment for block-major batched v2 acquisition: COMPLETE.
9. Sr Dev pure v2 plan/reconciliation/split/coverage foundation: COMPLETE.
10. Jr Dev foundation integration, migration 0017, and focused tests: COMPLETE.
11. Sr Dev implements bounded network concurrency and serialized persistence: COMPLETE.
12. Jr Dev integration of accepted engine, migration 0018, and focused tests: COMPLETE.
    0019 correction (composite acquisition/raw pairing FKs on engine events, canonical header
    receipts, and leaf receipts; upgrade-with-data and atomic-rollback tests; real
    crash-recovery and mixed-failure precedence engine tests) applied and pushed.
    Test-only follow-up replaces rejected placeholders with real claim/renewal, cached
    replay tamper, unsplittable-at-block, progressed-child, early `_route_failure`,
    exact retry-rollback, precedence-tuple, credential-free, and secondary engine-event
    mismatch coverage; all five v2 targets pass 155 focused tests. ACCEPTED at `ad30bf9`.
13. V2 live matrix, endurance pilot, full event/metadata acquisition, and
    `dex_pool_events`, `dex_pool_daily`, and `dex_universe_daily`: PENDING. Sol has
    authorized only the isolated matrix-harness source drop specified in
    `docs/handoff/CURRENT_TASK.md`; no RPC execution, integration, endurance, production
    acquisition, or publication is authorized. Sol rejected the first source drop after
    confirmed concurrency, raw-authentication, resume, safety-stop, output-isolation, and
    immutable-replay failures. The first correction fixed the original semaphore, equal-body
    race, raw-tamper, malformed-log, and production-store defects but remains rejected for
    pre-authentication catalog rewrite, incomplete resume authentication, broken live/replay
    pointer semantics, mutable report identity, indirect HTTP spooling, unscanned raw strings,
    and malformed-envelope demotion. The final Grok production/test-source correction and its
    frozen offline suite are integrated by Jr: the frozen matrix suite passes 32/32, targeted
    ruff passes, repository control passes, and an offline plan-only probe derives matrix ID
    `mtx_29211422a0ea5148c1601d39d647e916a57c3227d78026289685a6fb910901c2`, matching the frozen
    contract. Plan-only is non-PASS by design; no live matrix execution is authorized. Sol
    rejected commit `c9819c2` after direct probes proved plan/catalog tamper acceptance, complete
    evidence rollback, state-DB recreation before authentication, stale-run pointer restoration,
    and unscanned credential suffixes. The bespoke resumable matrix store is retired; a smaller
    immutable fresh-run-only production/test-source replacement is authorized in CURRENT_TASK.
    Sol rejected the first fresh-run replacement despite 23 passing tests: public probes proved
    unsealed JSON-RPC-error exits, cache-hidden raw tamper, forged receipt/raw acceptance, replay
    source/output overlap, and plan-only replay-source acceptance. A bounded terminal/authentication
    correction closes those five failures and its senior suite passes 29/29, but Sol rejected it
    against the already frozen contract after a public hard-wall probe exceeded a 0.05-second
    budget and review found that stop/drain, scanner-boundary, replay source-immutability/symlink,
    provider-retry, exact terminal-metrics, and CLI failure-path tests still do not execute the
    required production paths. The next bounded correction adds those probes and closes the prior
    scanner, symlink, retry, inventory, and metrics gaps, but Sol rejected it because the CLI test
    fails to import `scripts`, streamed responses can still exceed the absolute wall budget, and a
    timed-out injectable provider is abandoned rather than drained before terminal sealing. One
    narrow correction in the same three matrix files is pending. That correction fixes the CLI
    import and drains cooperative provider-call timeouts, but Sol rejected its stream-wall path:
    terminal return can report zero active operations while an unjoined daemon thread remains
    blocked in `next(iter_bytes)`. One final stream stop/close/join correction was pending. That
    final correction is now delivered in the working tree (source and tests, 2026-08-01 21:44):
    `stream_to_receipt` joins its non-daemon chunk reader on every path including wall expiry
    before decrementing provider-operation accounting or sealing a terminal. Sol accepts the
    uncommitted three-file source drop after five decisive tests and controls passed. Commit
    `5a5bfd1` contains only records; Jr integration, the complete 33-test suite, and a new
    commit/push containing the actual production source, CLI, and tests are pending. Jr integrated
    the accepted three-file drop unchanged: the matrix suite passed 33/33 in 412.43s, targeted ruff
    passed, repository control passed, and diff checks showed no engine/foundation/migration
    changes. No RPC call was made. Sol accepts the pushed integration at commit `0002b70`; the
    immutable fresh-run matrix harness is accepted. Sol authorizes exactly one fresh live matrix at
    2 requests/second/provider and 2 in-flight/provider, followed by one standalone replay only on
    live COMPLETE/PASS, under the exact preconditions, commands, evidence, and immediate stops in
    CURRENT_TASK. Endurance and production acquisition remain unauthorized. The resulting live
    evidence was COMPLETE but non-PASS and requires a nested-prefix capacity-selection redesign.
    Sol reviewed and accepted an uncommitted correction implementing that redesign, but Jr
    accidentally destroyed the drop with `git reset --hard HEAD` before integration. Current matrix
    files are byte-identical to `0002b70`; pre-reset bytecode is not an acceptable unchanged source
    drop. The unavailable source acceptance is withdrawn. ADR-0015 retains the accepted architecture,
    and CURRENT_TASK authorizes Sr only to redeliver the exact prior drop if available in its context
    or otherwise produce a clearly identified fresh regeneration for a new Sol source review. No Jr
    integration, test execution, replay, live RPC, endurance, or production acquisition is authorized.
    Sr's fresh regeneration is rejected after source review: generic authentication trusts sealed
    cells instead of recomputing them from retained evidence; unsuccessful batches can misclassify
    quota/auth failures as capacity; canonical cell topology is not authenticated; process-global
    scanner state can be replaced or cleared by concurrent harnesses; and required public-path and
    cross-process regressions are absent. CURRENT_TASK authorizes one bounded correction in the same
    production-source and senior-test files. Sol must re-review and record new file hashes before Jr
    integration. The first correction closes raw-evidence authentication, topology, scanner ownership,
    dual-provider evaluation, and public cohort-8 execution but remains rejected: final-attempt-only
    classification can hide earlier hard blockers; generic timeout text is accepted as capacity;
    injected transports can omit exact scanner inputs; generic path tails can be overclassified; and
    the required generated non-PASS, scanner, scheduler, and sealing-boundary regressions are not
    decisive. The next correction closes those production batch/scanner/timeout paths, but source
    review found one remaining scalar-side defect: a capacity-classified no-success scalar can mask
    the second provider's hard blocker even though every scalar reference is mandatory. Several new
    regressions are also statically invalid or incomplete, including exact-endpoint allowance and a
    child process that cannot reach lock acquisition. CURRENT_TASK authorizes only the listed scalar
    correction and decisive test repairs in the same two files. The next drop fixes those named paths
    but remains rejected because missing scalar evidence is converted to ordinary failed cells and can
    seal COMPLETE, stronger scalar-side blockers can collapse to generic scalar failure, malformed
    scalar interpretation still short-circuits provider inspection, and the mixed-provider batch test
    never reaches its batch evidence. The next drop closes those items but remains rejected because
    missing scalar evidence can still be hidden when the same side also has a stronger blocker, and
    malformed-continuation handling suppresses raw-integrity safety stops. Its FAILED-terminal test
    fabricates evaluator output rather than exercising missing production receipts. CURRENT_TASK
    authorizes only these residual source and test corrections. Sol accepts the resulting recovery
    drop after source review at production SHA-256
    `1782779e6fd9e99f60565568500271adf5313c5625029cecb21b70b1978ad5bb` and senior-test SHA-256
    `283b3029ec4c94f89239507b15d8436327f1e40ad5157b5afcac8df5c8478d74`.
    Targeted ruff, repository control, and diff checks pass; Sol ran no pytest or RPC. Jr is authorized
    to verify the hashes, integrate the unchanged drop and aligned three records, execute the complete
    matrix suite and controls, record exact evidence, and push only the five DEX-003 files. Live and
    downstream work remain unauthorized pending separate Sol decisions. Jr's first targeted execution
    collected only the 401/403 regressions and both failed. Ordinary `HTTP_401`/`HTTP_403` details are
    broadly rewritten to `redacted_credential_or_endpoint`, so authorization failures are
    misclassified as credential failures. Selection still hard-blocks, but the evidence taxonomy and
    accepted tests fail. Sol withdraws source acceptance and authorizes Sr only for the narrow scanner/
    authorization-classification correction in CURRENT_TASK. The resulting correction fixes ordinary
    401/403 classification and real credential redaction, but source review finds one final precedence
    defect: `http_429` is checked before persisted `credential_detection`, allowing actual credential
    evidence to be demoted to quota. CURRENT_TASK authorizes only the one-ordering correction and its
    mixed credential/429 regression. Sol accepts the resulting correction at production SHA-256
    `af7c1a24d283809ca2a9235133d83116d2ce4bb939a35a86198aa244bd9d05d0` and senior-test SHA-256
    `afef397a02ee651542678f19d87f0c01ee55cd21f24d27e67056ca5bbdb6e2f8`.
    Jr is authorized to run the three authorization regressions first and, only if they pass, the full
    matrix suite and controls before a five-file DEX-003 commit/push. Jr's full suite reached 100% with
    one failure: response-body credential detection persisted the safe but noncanonical marker
    `credential_or_endpoint_detected` instead of `redacted_credential_or_endpoint`. No secret leaked
    and classification remained a hard credential blocker. Sol withdraws acceptance and authorizes Sr
    only to normalize that persisted marker; the accepted senior test remains unchanged. Sol accepts
    the literal correction at production SHA-256
    `9f84dd007264372ed6499ba3782c0bb34ae0b83090acbf6ed31ff62d715d6a42`; the unchanged senior-test
    SHA-256 is `afef397a02ee651542678f19d87f0c01ee55cd21f24d27e67056ca5bbdb6e2f8`.
    Jr may rerun the single failed test and, only if it passes, the full suite and controls.
14. Sol 5.6 High performs final data-validity acceptance only after every blocking gate is
   PASS. No second Opus pass.

## Stop Condition

Sol accepts the authenticated clean matrix and standalone replay recorded in CURRENT_TASK and at
pushed commit `38b8d70`. Cohort size `8` is accepted only as a matrix result. All endurance-design
submissions are rejected, and the endurance-harness design phase is stopped for architecture redesign.
Sol now selects the staged full-production architecture in ADR-0015 section 9.10. Jr Dev - Hermes must
publish the ADR and two aligned records. Only the bounded offline production-foundation source/test phase
defined in CURRENT_TASK is authorized afterward; no controller, CLI, RPC, production execution, DEX
experiment, publication, or next ticket is authorized.

Jr executed the authorized live matrix on 2026-08-02T19:11Z with the frozen runtime options. Two
concurrent live runs occurred (procedural error: a `setsid`-forked survivor ran alongside a
foreground rerun). Both completed with status COMPLETE and `pass=false`: run
`run_5ed38a9ada6942d5964eeb622963d2d5` (5 pass / 10 fail; attempts 1,988; 429s 451; evidence hash
`db083430a7ac7af27258eb8f5087c11d9a40c899312a578da4187661e869a115`; report hash
`970639d0e0d6d4a879844f87d6693aee8c14c556934b5c765d76e5ecdb29e43b`) and run
`run_70e886dd31674d259042d11ac4194763` (7 pass / 8 fail; exit 1; attempts 1,942; 429s 411; evidence
hash `dbd4591046b72783854e2ca2f90703c78f1d1f57f0e07774c9e481d7bcee121d`; report hash
`69863959d13254a69dbc4b49975f21c0a14035f3c28ec598f70b8e5770712b1f`). Common blockers:
`credential_detection` on several Infura scalar calls and `provider_limit_or_size` /
`body_size_pressure` on medium-cohort cells; both runs report credential scan pass. No replay was
run (PASS precondition not met); no other live run is retained. Full credential-free evidence is in
CURRENT_TASK.

Sol accepts the corrected recovery integration at pushed commit `90c4082`: exact five-file scope,
accepted source/test hashes, 74/74 complete matrix suite, targeted ruff, repository control, and diff
checks. Live confirmation is not authorized because the previously recorded immutable live evidence
root and all three incident run IDs are absent from the active repository tree. The owner must provide
their current active location or report them lost; CURRENT_TASK governs the retention stop.

The owner confirms the three prior run directories were lost. Their committed summaries remain audit
records but receive no capacity, replay, endurance, or coverage credit. CURRENT_TASK authorizes Jr to
push this record-only incident update and then execute exactly one foreground live confirmation at
0.5 requests/second/provider and one in-flight/provider, with one standalone replay only on COMPLETE
and PASS. No rerun, detached execution, endurance, production acquisition, or downstream work is
authorized.

The fresh confirmation at record commit `89a8491` sealed FAILED before any matrix cell call when the
BlockPI chain probe exceeded the 60-second HTTP budget. Infura chain evidence was retained; 429 count
was zero; all cells are incomplete; no replay or capacity credit applies. Legacy trading-bot services
were active or enabled during the attempt, violating isolation, although their causal role is unproven.
The owner stopped/disabled them and Sol verified `trading-bot-data.service`,
`trading-bot-dex-sampler.service`, `trading-bot-dex-backfill.service`, and
`collector-daemon.service` are inactive and disabled with no matching process. CURRENT_TASK authorizes
one separately named clean replacement at the same 0.5 requests/second/provider, one-in-flight, and
60-second request limits. Any replacement failure ends the phase; no third run is authorized.

The replacement authorization is subsequently suspended. The owner disclosed that orphan PID 2687
was still running legacy `dex_ohlcv_sampler.py` during the failed window despite its service later
appearing inactive/disabled, and Sol observes Grok PID `2309342` consuming about 87% CPU. Process-level
quiescence, not service state alone, is now mandatory. No replacement record publication or RPC is
authorized until the owner stops the high-load process and Sol verifies the full process preflight in
CURRENT_TASK.

Sol's fresh process-level preflight now passes: both Grok PIDs and all named legacy acquisition
commands are absent; all four legacy units are inactive/disabled; replacement roots are absent; and
free disk exceeds 189 GB. The one clean replacement authorization is reactivated under CURRENT_TASK,
subject to the same process-level check immediately before execution. Any failure ends the phase with
no third run.

Jr executed the single authorized foreground live confirmation at 0.5 requests/second and one
in-flight per provider. Terminal FAILED via MatrixSafetyStop (global wall-time budget breached during
HTTP, timeout 60s) on the BlockPI chain probe; Infura chain probe succeeded. All 15 cells incomplete;
no capacity selection; no replay (not COMPLETE+PASS); no credit. Full credential-free evidence in
CURRENT_TASK.

The clean replacement live run run_f2fd323fcd69403a923f6329b9f0c320 sealed COMPLETE and PASS at
0.5 requests/second, one in-flight, 60s HTTP timeout (exit 0, 1803.7s, 1568 logical calls, 1580
provider attempts, 0 x 429, retained 144,991,333 bytes, credential scan pass). 12 of 15 cells PASS
with provider agreement; medium:cohort32/64/128 are capacity FAILS; capacity selection valid with
suggested cohort 8. The standalone replay run_bd066d2e228d46728a97fdb61138e365 also sealed COMPLETE
and PASS (31.4s) with identical cell and capacity decisions. Both output trees are preserved. Full
credential-free evidence in CURRENT_TASK.

Sol independently authenticates both preserved trees read-only and accepts the clean matrix/replay
evidence at record commit `38b8d70`. The three medium larger-prefix failures are authenticated capacity
boundaries: both mandatory scalar sides succeed and agree, while both batch sides fail capacity. Cohort
`8` is accepted as the initial-cohort input to the next bounded design phase only. CURRENT_TASK
authorizes Sr Dev - Grok Build to produce one uncommitted endurance-harness design proposal covering
pilot identity/isolation, representative scheduling, exact 20x throughput accounting, seven-/fourteen-
day and 2x-storage projections, bounded resources, hard-stop/resume semantics, and proposed source/test
scope. No source edit, test execution, RPC, endurance run, production acquisition, or downstream work
is authorized.

Sr delivered the authorized one-file endurance-harness proposal at SHA-256
`f6c343b3ccb02fe1cea783c924bffa1db58683ec336e8e6bbf58e81c0e0d87e6`. Sol rejects it before
implementation. Its `eplan_` identity is incompatible with accepted validators, records, and schema;
the proposed no-migration bootstrap is incomplete; the 25,000-root ceiling cannot sustain the required
seven-day-equivalent SEU rate for six hours; late strata exceed the authorized pre-2025 span; net
throughput is charged split/retry amplification twice; PASS inconsistently uses fourteen rather than
seven days; downtime can satisfy the purported monotonic six-hour gate; response bodies are not scanned
for credentials before persistence; terminal evidence is not completely sealed/authenticatable; and
required provider/resource metrics are not exposed. CURRENT_TASK authorizes only a correction to the
same uncommitted design file after Jr publishes the two-record review. No implementation, test, RPC,
endurance execution, production acquisition, or downstream work is authorized.

Sr's corrected one-file proposal has SHA-256
`51f36d9e1f9c9f36bd0ea868d9d4f4f25b8ebaba79bc1b31130b8d85e29924c7`. Sol rejects it before
implementation. Root selection is round-robin but the accepted engine executes chronologically;
per-stratum rates divide by total wall and are then weighted a second time; projection uses an arithmetic
rather than time-additive/harmonic combination; unobserved post-pin work is assigned an unsupported
rate; migration 0020 and schema-v2 records remain alternatives rather than an exact populated upgrade;
SEU identity uses rounded float arithmetic; append-only clock rows are mutated; credential bytes can
reach spool before a cross-chunk match; and terminal inventory/hashing remains ambiguous. CURRENT_TASK
authorizes one more correction to the same design file after Jr publishes this two-record re-review.
No source, migration, test, RPC, endurance execution, production acquisition, or downstream work is
authorized.

Sr's second correction has SHA-256
`e7787881247e30507b351dc53810f46da57c2659f9d9eac0f495697963d098e3`. Sol rejects it before
implementation. Its 2-RPS/2-node ideal ceiling projects the full lattice to 10.74 days, so seven-day PASS
is unreachable; its stratum quantiles/bands, duplicate mandatory picks, optional continuation, and
concurrent child priority do not define one execution identity; migration 0020 relies on an ineffective
in-transaction foreign-key pragma and omits the complete populated parent/child rebuild; elapsed-cost
accounting drops execution gaps and sub-millisecond time; resume/raw semantic authentication and sealing-
crash recovery remain incomplete; terminal wall time is not independently derivable; and scanner,
filesystem inventory, and disk-byte reconciliation remain ambiguous. CURRENT_TASK authorizes only one
final correction to the same untracked design file after Jr publishes this review. No implementation,
migration, ADR, tests, RPC, endurance execution, production acquisition, or downstream work is authorized.

Sr's final proposal has SHA-256
`7369431fbe10d52d7c3c4f31f48325385b7d2352026426a042ce88c2fb3013f2`. Its registry schedule anchors
reconcile, but Sol rejects it before implementation. Unique schedule ranks plus the minimum-open-rank
barrier serialize ordinary roots and invalidate the eight-node reachability proof; the barrier also
deadlocks on accepted PENDING-at-max terminal nodes. Proposed 8/8 concurrency is not the accepted
four-in-flight engine default, and the D=24 schedule leaves only 55.5 ideal seconds at four roots/second
for all overhead. Migration 0020 remains shorthand rather than complete FK-on DDL. Elapsed-cost samples
are not durable or exactly allocated; finalization cost is circular; shared-lock resume can race; raw/row
semantic authentication is incomplete; and clock, degraded sealing, scanner, terminal hash, and disk-byte
contracts remain non-executable. CURRENT_TASK records the full findings. The endurance-harness design
phase is stopped for architecture redesign; no further same-file correction or implementation is
authorized. Jr must publish only the two decision records. All live and downstream work remains
prohibited, and next ticket remains `NONE`.

Following pushed commit `74975e6`, Sol retires the separate endurance harness and selects a staged full-plan
production architecture. The complete cohort-8 plan is pinned at
`plan_2b96356463410b9d0a3f4f7313a06260360853207ed1bf1e42eec9eb4d756584`, 1,858,348 roots,
148,506,716,734 pool-topic-blocks, and root-domain-set SHA-256
`081a12f780d065a7596ba073ba80819d173e8d74b3b16235672da673942ea907`. No subset may share that
identity. ADR-0015 section 9.10 separates durable dual-agreed log candidates from globally batched,
deduplicated canonical headers, then finalizes authoritative leaves atomically. Hash-ordered full-plan
claims replace pilot ranks; streaming initialization, pre-spool credential scanning, rolling replenishment,
aggregate resource metrics, and exact root authority are mandatory. Jr must first publish the ADR and
records. Sr is then authorized only for the six-file offline production-foundation source/test phase in
CURRENT_TASK, including additive migration 0020; no accepted table rebuild is allowed. Controller/CLI,
live readiness preflight, RPC, staged production, coverage credit, publication, and downstream work remain
unauthorized. Next ticket remains `NONE`.

Sr's first six-file production-foundation drop is rejected at source review. The scope is correct, but
the production header-batch phase is absent; candidate/header finalization trusts database fields instead
of replaying and authenticating retained raw evidence; an interrupted initializer can authenticate the
chain before the root manifest is READY; anchor and finalization paths materialize all 1,858,348 domain
IDs; required migration/engine tests are missing, skippable, or vacuous; most new production metrics have
no update sites; and targeted ruff reports five findings. The exact reviewed hashes and full blockers are
recorded in CURRENT_TASK. Repository control and `git diff --check` pass; no pytest, migration, RPC,
production-data mutation, or Git operation was performed by Sol.

Jr must publish only this two-record rejection, excluding the uncommitted six-file source drop and all
unrelated working-tree changes. After publication, Sr may correct only those same six files against the
unchanged frozen contract and must stop for a fresh Sol source review with new hashes. Jr integration and
test execution, controller/CLI work, live readiness, RPC, staged production, coverage, publication,
downstream work, PAPER, LIVE trading, and any next ticket remain unauthorized. Next ticket remains
`NONE`.

Sr's second production-foundation correction remains rejected. Batch requests now pass spool/raw
persistence, but canonical-header `store_header` and `load_header` still invoke scalar-only raw replay,
so a batch-backed header fails before storage and the new batch cache verifier is unreachable. Global
header/finalization inventory uses unbounded `fetchall()` lists and an unbounded synchronous pass; claim
selection repeatedly replays the candidate prefix inside a write transaction; READY authentication does
not recompute root identities from row payloads; synchronous header work delays node replenishment; and
the senior suite still substitutes monkeypatched generic plans and omits required crash/tamper/atomicity
and exact-metric paths. CURRENT_TASK records the exact hashes and complete findings. Targeted ruff,
repository control, and `git diff --check` pass; Sol ran no pytest, migration, RPC, production-data
mutation, or Git operation.

Jr must publish only this two-record second re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a third correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's third production-foundation correction remains rejected. Scalar-or-batch canonical-header replay,
semantic READY-root recomputation, bounded returned work pages, and useful tamper/batch-metric tests are
retained. Claiming still restarts at the first PENDING row and performs an N+1 scan of the full candidate
prefix under `BEGIN IMMEDIATE`; its authentication cache grows without bound. Header scheduling performs
two global distinct-block counts per turn, and finalizable discovery may scan the complete candidate
population. Completed node slots are still not refilled until after synchronous header work. Required
READY-root tamper, candidate commit crash, forced in-transaction finalization rollback, refill-order, and
exact production-loop metric tests remain absent or non-decisive. CURRENT_TASK records the exact hashes
and complete findings. Targeted ruff, repository control, and `git diff --check` pass; Sol ran no pytest,
migration, RPC, production-data mutation, or Git operation.

Jr must publish only this two-record third re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a fourth correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's fourth production-foundation correction remains rejected. The indexed anti-candidate claim seek,
O(plans) marker, refill-before-header implementation, keyset finalization scan, and forced post-leaf
rollback test are retained. The first claim still performs a complete raw replay of every candidate, and
every later claim performs a full-table candidate count. Missing-header discovery never advances its
keyset cursor and repeatedly scans covered history. `header_backlog` is capped at one 32-row page rather
than exact. The production metric test remains inequality/disjunction based; the candidate crash test
expects an exception that `process_one` routes into a retry; READY tamper does not execute the READY
resume gate; and the refill test proves only initial capacity fill. CURRENT_TASK records the exact hashes
and complete findings. Targeted ruff, repository control, and `git diff --check` pass; Sol ran no pytest,
migration, RPC, production-data mutation, or Git operation.

Jr must publish only this two-record fourth re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a fifth correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's fifth production-foundation correction remains rejected. Resume replay is now keyset-paged, but
claim SQL still excludes every candidate rather than only authenticated candidates, so a tampered row
beyond the first page suppresses reacquisition. Exact header backlog is a full process-local Python set:
its first use performs an unbounded complete scan/materialization, it can run inside candidate commit's
write transaction, and it is stale across engine processes. Tests omit beyond-page tamper, bounded large
backlog, and cross-process coherence; the one-root metric test still uses inequalities for known values.
CURRENT_TASK records the exact hashes and complete findings. Targeted ruff, repository control, and
`git diff --check` pass; Sol ran no pytest, migration, RPC, production-data mutation, or Git operation.

Jr must publish only this two-record fifth re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a sixth correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's sixth production-foundation correction remains rejected. Durable transactionally updated backlog
rows/counter and exact one-root metric assertions are retained. Candidate auth rows, however, survive
restarts and are never revalidated: normal resume and `force=True` both skip every marked candidate, so
post-commit row/raw tamper can suppress reacquisition. The existing tamper test therefore cannot pass,
and the named beyond-page test uses only two candidates. Backlog fallback still runs a complete distinct
candidate-block insert plus complete backlog count under one write transaction. CURRENT_TASK records the
exact hashes and complete findings. Targeted ruff, repository control, and `git diff --check` pass; Sol
ran no pytest, migration, RPC, production-data mutation, or Git operation.

Jr must publish only this two-record sixth re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a seventh correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's seventh production-foundation correction remains rejected. Current-session candidate marks,
effective marked-candidate force replay, and bounded zero-state backlog initialization are retained.
Candidate auth nevertheless adds one permanent row per candidate per coordinator session with no bounded
cleanup; normal claims delete/reacquire the first valid candidate beyond each auth page; and `force=True`
still drains the whole population in one persistence command. The beyond-page test uses only three rows
with `limit=1` and private lifecycle mutation, and does not prove default-page preservation or bounded
session state. CURRENT_TASK records the exact hashes and complete findings. Targeted ruff, repository
control, and `git diff --check` pass; Sol ran no pytest, migration, RPC, production-data mutation, or Git
operation.

Jr must publish only this two-record seventh re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make an eighth correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's eighth production-foundation correction remains rejected. Removing durable candidate-by-session rows,
preserving valid candidates, and returning after one forced authentication page are retained. Claim SQL,
however, excludes every candidate before current-session replay reaches it. The process-local lexicographic
watermark has no multi-process generation boundary, so a candidate inserted behind a live/completed cursor
is never normally replayed but still suppresses reacquisition. Authentication advances only through claim
attempts and can stop without reaching a safe boundary. The required 32+1 restart test still mutates private
engine lifecycle fields, uses one replacement coordinator and a manual force loop, and proves neither public
resume nor repeated/multi-process bounded state. CURRENT_TASK records the exact hashes and complete findings.
Targeted ruff, repository control, and `git diff --check` pass; Sol ran no pytest, migration, RPC,
production-data mutation, or Git operation.

Jr must publish only this two-record eighth re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a ninth correction only in the same six files against the
unchanged frozen contract and must stop for fresh Sol source review. The correction must make only
current-session-authenticated candidates authoritative for exclusion with bounded race-safe multi-process
state, drive bounded authentication to a public safe boundary even without other work, and replace the
private-field test with decisive public 32+1/repeated/concurrent/insertion-behind-cursor coverage. Jr
integration/testing and all controller/CLI, live readiness, RPC, staged production, coverage, publication,
downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's ninth production-foundation correction remains rejected. Its one-generation-per-candidate plus
one-row-per-plan protocol, stale/null selection, fail-closed candidate deferral, bounded force page, public
attach method, and independent auth turn are retained. The attach method nevertheless bypasses immutable
plan/execution-policy matching and can resume under changed authority settings. Run completion is an
unversioned local Boolean that is not invalidated when another coordinator bumps the shared generation, and
an auth page can stamp/report completion for a generation changed during replay. The purported concurrent
public test is sequential, never calls `run_until_idle`, simulates insertion by clearing an existing mark,
contains vacuous progress/state assertions, and omits policy-mismatch and generation-change recovery paths.
CURRENT_TASK records the exact hashes and complete findings. Targeted ruff, repository control, and
`git diff --check` pass; Sol ran no pytest, migration, RPC, production-data mutation, or Git operation.

Jr must publish only this two-record ninth re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make a tenth correction only in the same six files against the
unchanged frozen contract and must stop for fresh Sol source review. Attach must authenticate the persisted
plan/policy before mutation; page and run completion must bind to and recover from concurrent generation
changes; and decisive public tests must overlap real coordinators, exercise a public insert behind progress,
prove old-runner recovery through `run_until_idle`, assert exact bounded state, and reject changed attach
authority. Jr integration/testing and all controller/CLI, live readiness, RPC, staged production, coverage,
publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's tenth production-foundation correction remains rejected. Plan/most-policy attach authentication,
generation rechecks/restart results, per-turn generation-bound scheduling, and threaded public lifecycle calls
are retained. Attach still derives expected claim order from the stored policy itself, so production
`domain_hash_v1` is not independently pinned. The new concurrency test cannot reach its asserted boundaries:
both runners enter the logs-first header-batch loop without installing the batch-aware RPC fixture, while the
default handler calls `.get` on the request list. It also bumps only after the first run returns rather than
during a page, never asserts the restart result in one running invocation, and does not tie its candidate
commit to an exact domain/auth transition behind known progress. CURRENT_TASK records the hashes and full
findings. Targeted ruff, repository control, and `git diff --check` pass; Sol ran no pytest, migration, RPC,
production-data mutation, or Git operation.

Jr must publish only this two-record tenth re-review, excluding the uncommitted six-file drop and all
unrelated changes. After publication, Sr may make an eleventh correction only in the same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Pin claim order independently,
install/exercise the real batch path, deterministically bump during a paused auth page and prove the same
running scheduler reopens/completes, and assert the exact public insert-behind row's generation transition
without deletion/reacquisition. Jr integration/testing and all controller/CLI, live readiness, RPC, staged
production, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

Sr's eleventh production-foundation source fixes are retained, but its decisive test remains rejected.
Attach now takes expected claim order independently, pins production `domain_hash_v1`, and retains the
plan/policy and generation-atomic corrections; no further production-source or migration edit is authorized.
The test's global `_authenticate_candidate_row` hook, however, catches commit-time authentication after B's
`process_one` has consumed the only stale initial row. A pauses while `commit_log_candidate` holds
`BEGIN IMMEDIATE`; B's public attach cannot acquire the write lock to bump generation, and the thread error
is re-raised before any restart/safe-boundary assertion. CURRENT_TASK records the hashes and exact trace.
Targeted ruff, repository control, and `git diff --check` pass; Sol ran no pytest, migration, RPC,
production-data mutation, or Git operation.

Jr must publish only this two-record eleventh re-review, excluding the uncommitted source drop and unrelated
changes. After publication, Sr may correct only
`tests/acquisition/test_uniswap_v2_pair_events_v2_engine.py`; the other five reviewed files are frozen. The
test must pause only inside resume-page replay before its stamp transaction, with a known stale candidate and
no SQLite write lock, then prove B's public bump is observed and completed by A's same running scheduler while
the exact public candidate survives and reaches the final generation. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE, and
next-ticket work remain unauthorized. Next ticket remains `NONE`.

Sr's twelfth, test-only correction completes source review of the production foundation. The five frozen
files are byte-identical; the engine test now leaves a known stale row, scopes its pause hook exclusively to
resume-page replay before any write transaction, allows B's public attach to bump mid-page, records the
explicit generation restart from A's same active `run_until_idle`, completes the new boundary, and proves the
exact public candidate survives at the final generation. Independent plan/policy/claim-order rejection, real
batch headers, the 32+1 boundary, and exact bounded authentication state remain covered. Sol accepts the exact
six hashes recorded in CURRENT_TASK for Jr integration. Targeted ruff, repository control, and
`git diff --check` pass; Sol ran no pytest, migration, RPC, production-data mutation, or Git operation.

Jr must verify and integrate the exact six-file drop, run the three focused offline test targets plus targeted
ruff/repository control/diff checks, and stop on any failure or hash drift. After focused PASS, Jr may apply
0020 through the repository migration mechanism to offline `dex003_full.db`, proving the migration history
checksum, preservation of every prior v2/raw table count, empty expected new tables, and empty
`PRAGMA foreign_key_check`. Jr records the evidence in both governance files, commits/pushes only the six
accepted files and records, and stops for Sol integration review. Controller/CLI, live readiness, RPC,
production acquisition, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain
unauthorized. Next ticket remains `NONE`.

Sr's first production-foundation correction is rejected at source re-review. The corrected drop adds
streamed root hashing, READY authentication, raw candidate replay, batch parsing, scanner-over-cap tests,
and metric update sites, but the batch path is non-executable: the spool descriptor rejects list requests
and persistence treats them as mappings. Batch-backed cache replay remains scalar-only; no global
candidate-block/header/finalization work loop calls the new public methods; candidate claim exclusion does
not authenticate candidates on resume; migration and engine tests still omit required populated tables and
decisive batch/candidate/finalization cases; the query-plan assertion does not exclude a temporary sort;
and in-flight/header-backlog metrics are not exact. CURRENT_TASK records the exact hashes and full findings.
Targeted ruff, repository control, and `git diff --check` pass; Sol ran no pytest, migration, RPC,
production-data mutation, or Git operation.

Jr must publish only this two-record re-review, excluding the uncommitted six-file source drop and all
unrelated changes. After publication, Sr may make a second correction only in those same six files against
the unchanged frozen contract and must stop for fresh Sol source review. Jr integration/testing and all
controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
and next-ticket work remain unauthorized. Next ticket remains `NONE`.

Jr integrated the Sol-accepted six-file drop after verifying all six hashes. The focused suite fails
3/128 (exit 1, 115.82s): the cohort-rejection test's expected regex `cohort size=8` does not match the
actual message `production root iterator requires initial_cohort_size=8`; the public finalize path raises
`sqlite3.IntegrityError: CHECK constraint failed: status IN ('PENDING','IN_FLIGHT','AGREED','SPLIT')` on
its leaf insert before the forced post-leaf rollback assertion; and the claim-index test joins
`sqlite3.Row` object reprs, so its `CLAIM_DOMAIN`/`USING INDEX` assertion cannot match. Targeted ruff, repo
control, and `git diff --check` pass; all six accepted hashes remain unchanged. No migration 0020 was
applied, no database was touched, and the six-file drop remains uncommitted exactly as reviewed. CURRENT_TASK
records the full evidence and log. Per the stop contract, Jr returns this evidence to Sol; no source/test
redesign, commit of the drop, RPC, or downstream work was performed. Next ticket remains `NONE`.

Sol classifies all three failures as Sr-owned test defects, not production-source, migration-SQL, Jr, or
environment failures. The wrong-cohort test must match the actual `initial_cohort_size=8` authority error;
the finalize rollback trigger must use an allowed non-PENDING node status so execution reaches the intended
post-write guard instead of violating migration 0017's status CHECK during leaf insertion; and the 0020 index
test must inspect EXPLAIN's detail column rather than `sqlite3.Row` object representations. Production source
and migration SQL remain accepted and frozen at the hashes in CURRENT_TASK.

Jr must first publish only this two-record routing decision. Sr may then correct only
`tests/acquisition/test_uniswap_v2_pair_events_v2.py`,
`tests/acquisition/test_uniswap_v2_pair_events_v2_engine.py`, and
`tests/acquisition/test_uniswap_v2_pair_events_v2_migration_0020.py`, limited to those exact failures, and
stop for fresh Sol source review. Jr rerun/integration, migration application, controller/CLI, live readiness,
RPC, production acquisition, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain
unauthorized. Next ticket remains `NONE`.

Sr's thirteenth, three-test correction is accepted for Jr rerun. Production source and migration SQL remain
byte-identical at their accepted hashes. The wrong-cohort test now matches `initial_cohort_size=8`; the
finalize rollback trigger uses allowed non-PENDING state `IN_FLIGHT` so the intended post-write engine guard
executes and rolls the transaction back; and the 0020 index test reads EXPLAIN's detail field instead of row
object representations. The new exact test hashes and static PASS evidence are recorded in CURRENT_TASK.

Jr must verify all six hashes, rerun the full three-target focused suite plus targeted ruff, repository
control, and `git diff --check`, and stop on any failure. On 128/128 PASS, Jr may apply migration 0020 to
offline `dex003_full.db`, prove its checksum/history, unchanged prior v2/raw counts, expected empty new
tables, and empty foreign-key check, then commit/push only the accepted six files and evidence records and
stop for Sol integration review. Controller/CLI, live readiness, RPC, production acquisition, coverage,
publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Jr integration run - accepted six-file drop fully integrated (2026-08-10)

Jr verified all six accepted SHA-256 hashes byte-identical to the reviewed drop, reran the full three-target
focused suite (128 passed, 0 failed, exit 0, 109.91s; log `logs/dex003/production_foundation_suite_run2.log`),
reran targeted ruff (all checks passed), repository control (PASS), and `git diff --check` (clean), then
applied migration 0020 to offline `dex003_full.db` through the repository migration mechanism and recorded the
exact checksum/history row, unchanged pre-0020 v2/raw counts, expected empty new tables, and empty `PRAGMA
foreign_key_check` (logs `logs/dex003/migration_0020_pre_evidence.log` and
`logs/dex003/migration_0020_post_evidence.log`). Full evidence is recorded in CURRENT_TASK.

Jr committed and pushed only the six accepted files plus both governance records, excluding `opencode.json`
and both untracked research files. Stopped for Sol integration review.

## Sol integration review - production foundation accepted (2026-08-09)

Sol accepts the offline production-foundation integration at pushed commit
`179233a484b251a10d18d1b2da360dc194a4f5ca`. Its scope is exactly the six accepted source/test files
plus the two governance records, all six accepted hashes remain unchanged, and `HEAD == origin/main`.
Jr's 128/128 focused result, targeted ruff PASS, repository-control PASS, and clean diff check are accepted.
Read-only database review confirms 20 migration-history rows; migration 0020 at accepted checksum
`f41175e3b23b24e8e5b5ba512a4fd10514201b1d156115664ff88f0442cb93bb`; preserved counts of 548,721
`raw_acquisition` and 509,711 `raw_object` rows; all six new tables present and empty; and an empty
`PRAGMA foreign_key_check`.

This completes only ADR-0015 section 9.10's offline production-foundation source/integration prerequisite.
No controller/CLI, live readiness, RPC, staged production, coverage, publication, downstream, PAPER, LIVE,
or next-ticket work is authorized by that acceptance alone. Jr Dev - Hermes must publish it together with
the subsequent ADR-0015 section 9.11 architecture decision in the three aligned governance files, excluding
unrelated working-tree files. Next ticket remains `NONE`.

## Sol architecture decision - production controller and staged authority (2026-08-09)

ADR-0015 section 9.11 now freezes the missing control plane after accepted foundation commit `179233a`.
It retains the accepted engine as the sole scheduler, isolates readiness from production credit, requires a
reviewer-bound capability for every production network/finalization boundary, adds exact PTB/clock/storage
checkpoint authority, and permits production only as separately reviewed stages.

Readiness identity `rdy_abadab41f5f4221a0f2e5c36e11b5bbe3893393dfab85e614be65ff2f26975bb`
binds the 128 lowest production domain IDs (digest
`7f009be09d1268008d69940078fea3e62264314d39754ed8f399537e283b90ea`, 10,240,000 PTB), the exact
six-rung capacity ladder, limits, reachability/storage formulas, evidence topology, and terminal authenticator.
No live readiness run is authorized.

Jr must first commit/push only `docs/adr/0015-data-first-dex-research-substrate.md`,
`docs/handoff/CURRENT_TASK.md`, and this ticket, excluding unrelated files. After publication, Sr Dev - Grok
Build is authorized for source and senior tests only in the seven files listed in CURRENT_TASK: the accepted
engine, new production controller module/CLI, additive migration 0021, existing engine tests, and new
production/migration tests. The implementation must follow ADR-0015 section 9.11 literally, including direct-
engine production-RPC denial, exact readiness selection, the nine additive control authorities, capability-
gated attribution/credit, stage clock/checkpoint/terminal authentication, and decisive offline tests.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch evidence or production
data, or use Git, and stops for Sol source review. Jr integration, live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - production controller rejected (2026-08-09)

Jr published the ADR-0015 section 9.11 architecture at `8a24672`; `HEAD == origin/main`. Sr's first
uncommitted controller drop is rejected. Exact reviewed hashes and full evidence are recorded in
CURRENT_TASK. The unchanged engine-test file remains at its accepted hash; the drop otherwise stays within
the authorized file boundary. Targeted ruff fails with seven unused-import/variable errors; repository
control passes and `git diff --check` is clean. Sol ran no pytest, migration, RPC, production-data, or Git
operation.

Blocking defects are architectural, not integration issues: both live-capable CLI commands always refuse
execution; readiness/rung/evidence/replay and production preparation are absent; any object with the
production `plan_id` bypasses the engine's in-memory capability check; durable lease/policy/permit authority
is never checked at engine boundaries; stage attempts and caller-supplied PTB credits are separate unused
transactions; the default controller clock never advances, leases can be stolen and cannot renew, and
checkpoint/drain/crash/deadline/storage rules are not implemented; sealing precedes later DB/event writes
and the authenticator checks only a manifest hash plus table presence; migration relationships allow cross-
plan/stage authority; and the shallow tests do not execute the frozen public paths.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files.
After publication, Sr may correct only the same seven files. The correction must implement ADR-0015 section
9.11 literally: real isolated readiness execution/replay, complete production prepare, durable capability
reauthentication at every engine boundary, lease renewal/no theft, atomic attempt/raw and registry-recomputed
leaf credit, exact controller clocks/checkpoints/storage/drain/crash/sealing/authentication, all seven gated
CLI paths, same-plan/stage migration authority, decisive public-path tests, and clean targeted ruff.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Control-plane state pointer (2026-08-17)

The authoritative current decision is the detailed section titled
`Sol integration review - fifteenth controller acceptance withdrawn (2026-08-17)` above. The fifteenth source
acceptance is withdrawn and migration 0021 was not applied. Jr Dev - Hermes is the next actor only to publish
the aligned two-record review. The sixteenth correction becomes available to Sr Dev - Grok Build at the recorded
Grok 4.6 Max escalation tier only after publication and must stop for fresh Sol source review. All Jr
reintegration/testing, live readiness, production preparation/RPC/stages, coverage, publication, downstream,
PAPER, LIVE, harmonic-model, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Sol integration review - fifteenth controller acceptance withdrawn (2026-08-17)

Jr published the source acceptance at record-only commit `c51e00b`, verified the seven accepted hashes unchanged,
and ran the authorized three-target focused suite. The run reached 100% with 146 passing and 31 failing tests;
exact log/evidence hashes and the complete failure classification are recorded in CURRENT_TASK. Migration 0021 was
not applied: the offline database remains at 20 migrations through accepted 0020, every pre-0021 v2 table is empty,
raw counts remain 548,721 acquisitions and 509,711 objects, and `PRAGMA foreign_key_check` is empty.

Sol withdraws the fifteenth source acceptance. Blocking production defects are: mismatched acquisition identity in
deferred atomic catalog/attempt registration; non-idempotent one-attempt lineage replay; no chain authentication in
the public stage lifecycle; a drain path that re-enters the acquisition loop; terminal-seal and prepare-baseline
rows left only in WAL and therefore absent to the required immutable reader; leaked readiness JSON errors; symlink
acceptance after premature path resolution; and redundant named indexes that the real SQLite query plans do not
select. Senior tests also contain conditional/fallback journal assertions, swallowed promised failures, invalid
constructor/state/identity assumptions, duplicate ordinal/plan fixtures, fabricated prior-terminal authority, and
paths that fail before the behavior they claim to test.

Jr must first publish only this aligned two-record rejection, excluding the seven-file drop and all unrelated files.
After publication, Sr Dev - Grok Build is authorized at the Grok 4.6 Max escalation tier to make a sixteenth
correction only in the same seven files. It must retain the independent authority-schema pin and coordinated forgery
defenses; make raw/attempt/lineage registration and real startup recovery atomic, stable, exact, and idempotent;
authenticate chain before stage work; drain only started work; make fresh terminal/ARMED outputs immediately
immutable-readable without WAL; reject malformed JSON and symlinks fail-closed; align useful natural query indexes;
and replace every conditional, fallback, fabricated, earlier-guard, or swallowed-exception test with a decisive
public path. No source weakening to accommodate invalid fixtures is allowed.

Sr does not run tests/migrations, edit records, use RPC/network, touch production/evidence data, or use Git, and
stops for fresh Sol source review with new hashes. Jr reintegration, migration 0021, all live readiness,
production preparation/RPC/stages, coverage, publication, downstream, PAPER, LIVE, harmonic-model, and next-ticket
work remain unauthorized. Next ticket remains `NONE`.

## Sol source review - third controller correction rejected (2026-08-10)

Jr published the prior review at `2bf1719a`; `HEAD == origin/main`. Sr's third correction touches all seven
authorized files but is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff
passes, repository control passes, and `git diff --check` is clean. Sol ran no pytest, migration, RPC,
production-data, or Git operation.

The correction authenticates exact registry parquet bytes and selects lowest root objects, but caller pool
births are not bound to that registry, matrix/source authority is shallow, and exact root-count/PTB authority
is absent. Rungs use a malformed synthetic non-production plan, private coordinator/engine state, and partial
success criteria rather than the complete public workload. Required safety, byte/storage, recursive evidence,
credential, and zero-network replay semantics remain absent. Generated readiness evidence hashes canonical
JSON while its authenticator hashes newline-terminated files, so it cannot authenticate itself and prepare
cannot consume a generated PASS. Prepare still does not initialize/authenticate/seal the production foundation.

Stage-run mutates resume state before capability binding, accepts paths not reconciled to policy, and double-
closes its handed-off lock fd. The controller still blocks renewal/checkpoints behind `run_until_idle`, uses
placeholder counters/projections, and cannot prove drain/crash/deadline/storage or immutable semantic sealing.
The unchanged engine records no stage attempts and does not transactionally authenticate birth-clamped leaf
credit. Migration/tests remain far short of the decisive authority, public lifecycle, populated-upgrade,
rollback, tamper, and exact-index matrix.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files.
After publication, Sr may make a fourth correction only in the same seven files. It must bind all canonical
registry/matrix/source/path authorities; run and replay complete independent rungs through public APIs; seal
self-authenticating recursive readiness evidence; fully prepare/READY the production foundation offline;
integrate atomic real-attempt and registry-birth credit authority; implement correctly owned locking plus
concurrent renewal/checkpoint/clock/storage/stop/drain/crash control; fully reconstruct readiness/stage semantics;
finish prior-terminal/composite schema authority; and add the complete decisive test matrix. Private state
mutation and caller substitutes are prohibited.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - fourth controller correction rejected (2026-08-10)

Jr published the prior review at `4d9888f`; `HEAD == origin/main`. Sr's fourth correction touches all seven
authorized files but is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff
passes, repository control passes, and `git diff --check` is clean. Sol ran no pytest, migration, RPC,
production-data, or Git operation.

Matrix authority still accepts forgeable three-field JSON instead of authenticating the retained accepted run;
source paths are arbitrary. Readiness does not enforce its wall/retained bounds or exact isolation, replay,
attempt/raw/split/header semantics, and its byte model uses pathname/equal-share proxies plus caller free space.
Its public authenticator can accept a self-consistent asserted tree without any rung database or raw evidence.
Prepare allows source substitution, PTB-as-byte storage, incomplete structural replay, and a mutable registry-
birth file whose hash is not bound to policy.

Stage-run opens SQLite before its OS lock. Six-hour/24-hour and exact integer clock rules remain unwired; crash
does not authenticate/RESUME. Engine attribution emits one separate synthetic primary summary rather than exact
per-provider/per-retry raw-paired attempts, and capability authentication is outside the atomic credit TX.
Checkpoint storage remains a static PTB proxy; a daemon control thread may survive its timed join; sealing writes
the terminal before later state/lease mutations and inventories no production DB/raw tree; authentication remains
partial and can fall back to mutable read-only SQLite. Migration and tests include named placeholders, including
a manual transaction rollback, nonempty-only query-plan assertion, failed-stage smoke, and an engine assertion
ending in `or True`.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a fifth correction only in the same seven files. It must authenticate complete matrix
and exact source authority, reconstruct full readiness evidence/replay and exact byte/storage selection, seal
authenticated prepare/birth authority, lock before mutable open, implement exact permit clocks/crash semantics,
persist exact raw-paired attempts and capability-gated credit transactionally, produce complete last-write
terminal sealing/semantic authentication, finish immediate-prior terminal authority, and replace all placeholder
tests with the decisive public-path/populated-migration matrix.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - fifth controller correction rejected (2026-08-10)

Jr published the prior review at `87ea0fd`; `HEAD == origin/main`. Sr's fifth correction touches all seven
authorized files but is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff,
repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git action.

The correction adds real public matrix authentication, exact source paths, stronger isolation, public readiness
work, partial raw attribution, pre-SQLite locking, integer clock arithmetic, a non-daemon controller, identity
recomputation, immediate-prior migration authority, and last stage-directory terminal creation. Blocking defects
remain: matrix identities are optional/fail-open; readiness wall/retained limits are checked only after blocking
work and its authenticator trusts summary eligibility rather than reconstructing full attempts/replay semantics;
database bytes are allocated by PTB and production/checkpoint gates still use PTB as a byte proxy.

Production prepare first confirms one policy ID, then binds births and demands the same confirmation equal a new
policy ID, so it cannot succeed. Stage-run never recomputes the policy-bound births core. Concurrent engine calls
share one mutable attribution context; missing/failed inserts are swallowed and no-response fallback cannot write.
First-start remains a forgeable standalone timestamp; crash has no authenticated RESUME. A live control thread may
survive into sealing; the DB is hashed before later DB mutations but that metadata is not authenticated; mutable
read-only fallback remains. Credit and attempt authentication is only positive-value/link/count checking. Tests
still lack successful public prepare/stage paths and retain manual rollback, nonempty-only query-plan, incomplete
immutability, forgeable-clock, concurrency, and seal gaps.

Jr must first commit/push only this two-record review, excluding the uncommitted seven-file drop and unrelated
files. After publication, Sr may make a sixth correction only in the same seven files. It must enforce live hard
bounds and mandatory matrix facts; reconstruct complete readiness/stage semantics; implement exact attributed
byte maxima without PTB proxies; compute the final birth-bound policy before confirmation; authenticate complete
birth authority at every boundary; make attempt attribution per-operation, transactional, concurrency-safe, and
fail-closed; use the same atomic capability/credit connection; bind clocks and CRASH/RESUME durably; join all work
before a recursively authenticated immutable last-write seal; and replace placeholders with decisive successful
public-path and adversarial tests.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - sixth controller correction rejected (2026-08-10)

Jr published the prior review at `4161ce6`; `HEAD == origin/main`. Sr's sixth correction changes four of the
seven authorized files but is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted
ruff, repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git
action.

Mandatory matrix identities, final birth-bound policy confirmation, live readiness stop requests, same-connection
credit reauthentication, immutable stage SQLite, and a partial byte/PTB maximum are improvements. The main new
attempt fix is nonfunctional: caller/node code stores context in `threading.local()`, but the separate persistence
thread reads its own empty thread-local and inserts nothing; the bound-capability fallback remains disabled.
Authenticated matrix cohort/provider facts are optional and replay facts are unchecked. The daemon watchdog can
survive its timed join, while readiness authentication and proportional database-byte attribution are materially
unchanged.

Prepare still lacks a public final-policy preview and durable non-armed failure. Checkpoints omit raw/spool trees,
do not seed readiness byte authority, and project zero with remaining work until a credited interval appears.
First-start is still forgeable, CRASH has no authenticated RESUME, and exact classifications remain unwired. A
still-live control thread sets an error then is passed directly into FAILED drain/seal. The DB manifest is captured
before later DB writes and authentication checks only its length, not SHA-256; recursive raw/controller authority
is absent. Birth authentication exceptions are caught and PTB recomputation is skipped. Migration and engine tests
are unchanged, preserving the manual rollback, nonempty query-plan, incomplete immutability, and cross-thread gaps.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a seventh correction only in the same seven files. It must carry attempt authority inside
each persistence command/descriptor and prove concurrent exact attribution; require complete live/replay matrix
facts; own/join the readiness watchdog and reconstruct immutable complete readiness semantics; replace every byte
proxy and carry the readiness maximum into full-tree checkpoints; add a public final-policy preview and durable
non-armed prepare failure; make births mandatory; bind first-start and CRASH/RESUME durably; refuse all sealing
while work is live; seal and verify the post-mutation recursive DB/raw/controller identity; and deliver the full
successful public-path, populated migration, concurrency, clock, storage, birth, and adversarial seal test matrix.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - seventh controller correction rejected (2026-08-10)

Jr published the prior review at `c375580`; `HEAD == origin/main`. Sr's seventh correction changes six of the
seven authorized files but is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted
ruff, repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git
action.

Descriptor-carried authority, concurrent persistence tests, final-policy preview, mandatory birth checks, improved
clock/control logic, post-mutation DB hashing, and expanded tests are real progress. Blocking defects remain. The
matrix code looks for nonexistent cohort keys instead of the accepted report's
`capacity_selection.selected_cohort_size`, so readiness rejects its actual authority. Raw catalog methods commit
before attempt insertion, and crash journals are written before production authority is stamped; raw evidence can
therefore survive without an attempt.

Readiness still treats all DB/receipt growth as fixed, accumulates fixed bytes across rungs, permits a zero PASS
rational, and does not replay complete evidence. The real stage CLI never seeds the readiness rational. NON_ARMED
is an unchecked overwriteable file while policy/READY state remains runnable. A forged full first-start file is
accepted, and live-thread cleanup is tested only with a literal result dictionary. Stage sealing stores `core_hash`
durably but its authenticator compares that row to the full MANIFEST hash, so every generated stage self-rejects;
recursive tree entries are not verified. Query-plan tests still accept generic table/scan fallbacks.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make an eighth correction only in the same seven files. It must use the real matrix report
shape; make journal authority durable and raw+attempt persistence atomic through recovery; reconcile every raw
acquisition; implement exact positive readiness byte authority and load it in the real stage CLI; make ARMED/
NON_ARMED immutable network authority; reject a forged full first-start and implement new-permit RESUME; safely
retain ownership when work remains live; use one self-consistent recursively verified stage seal; and add successful
public paths plus exact adversarial crash, rollback, arming, clock, tree, and query-plan tests.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - eighth controller correction rejected (2026-08-10)

Jr published the prior review at `1124622`; `HEAD == origin/main`. Sr's eighth correction changes six of the
seven authorized files; migration 0021 remains unchanged. The correction is rejected. Exact hashes and full
evidence are recorded in CURRENT_TASK. Targeted ruff, repository control, and `git diff --check` pass. Sol ran
no pytest, migration, RPC, production-data, or Git action.

The real matrix report shape, pre-network journal authority, positive readiness rational/CLI seed, forged-file
candidate handling, live-thread ownership, and recursive inventory are meaningful corrections. Production-bound
persistence nevertheless fails before its transaction because it assigns the read-only real-SQLite `commit`
method. Its reconciliation then ignores failed/no-response orphans but tries to claim all historical successful
raw evidence for one stage, so it is neither complete nor production-scoped.

Readiness still estimates SQLite group bytes by proportional raw-byte share rather than exact row/index deltas.
ARMED does not durably bind or authenticate initialized database/tree bytes or READY anchors, and NON_ARMED cannot
atomically undo or safely supersede prior policy/READY mutation. A same-plan/same-wall first-start file still skips
stage/policy/permit/controller reconciliation, the file-before-START crash window bricks recovery, and no new-stage
RESUME/prior-terminal path exists. The durable stage core excludes database identity, leaving the full hash only in
editable JSON, while symlink tree orphans are ignored. Semantic replay and decisive tests remain incomplete; the
rollback test targets an exception the real connection will not reach, the matrix test is a dict assertion, and
leaf-credit query-plan coverage still permits a generic fallback.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a ninth correction only in the same seven files. It must use a supported atomic raw/attempt
transaction and exact stage-scoped lineage including failures/recovery; measure exact per-group raw/receipt/SQLite
deltas; make schema-bound ARMED/NON_ARMED authority include initialized bytes and READY; atomically bind START and
new-permit/prior-terminal RESUME; durably bind the post-mutation DB/full manifest and reject every recursive tree
orphan/link; reconstruct complete semantics; and replace placeholder coverage with successful public end-to-end
and exact adversarial/index tests.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - ninth controller correction rejected (2026-08-10)

Jr published the prior review at `aae629f`; `HEAD == origin/main`. Sr's ninth correction changes the engine,
production-controller, and engine-test files and is rejected. The CLI, migration, production tests, and migration
tests remain byte-for-byte at their rejected eighth-correction hashes. Exact hashes and full evidence are recorded
in CURRENT_TASK. Targeted ruff, repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC,
production-data, or Git action.

The catalog-connection proxy removes the direct assignment to SQLite's read-only `commit` method and adds stage
lineage to response metadata/outcome JSON. The controller partially consumes that lineage, strengthens START
payloads, emits a provisional RESUME, requires ARMED byte fields, and moves database metadata into the core. The
transaction is still unsafe: catalog rollback tears down the outer transaction, every `BEGIN IMMEDIATE` error is
silently accepted, and commit-before-cleanup recovery plain-inserts the same deterministic attempt ID forever.

Lineage authentication checks only partial JSON and does not require exactly one failure link or reconcile complete
attempt identities. Readiness retains proportional DB bytes. ARMED initialized bytes remain outside policy and full-
tree/READY authentication. START retains its file-before-row crash window; RESUME accepts no prior terminal or
permit binding. The seal hashes SQLite before inserting its terminal row, so its own generated DB identity is stale.
The widened generic-exception test covers none of these production-controller paths, recovery idempotence, successful
atomic commit, schema-bound lineage, or exact historical scoping.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a tenth correction only in the same seven files. It must use a real explicit transaction
whose catalog conflict paths cannot split raw/attempt atomicity; make exact post-commit journal replay idempotent;
schema-bind and fully cross-authenticate successful and failed lineage without claiming unrelated history; measure
exact readiness group DB/index deltas; make initialized-tree/READY ARMED authority transactional; make START crash-
safe and RESUME require the authenticated prior terminal/new permit; solve post-terminal database sealing without
self-reference; and deliver the missing exact migration/index and successful public-path tests.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - tenth controller correction rejected (2026-08-10)

Jr published the prior review at `0d70763`; `HEAD == origin/main`. Sr's tenth correction changes all seven
authorized files and is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff,
repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git action.

The correction now independently measures readiness-group raw/SQLite deltas, schema-binds prepare outcomes and
stage acquisition lineage, authenticates deterministic attempt replay, binds RESUME permits to a prior terminal,
and generates a seal that authenticates its current database hash. The production atomicity change nevertheless
begins a SQLite write transaction before physical raw streaming. Lease renewal serviced during that stream returns
success on the same connection but is not durable or externally visible until persistence finishes; the decisive
heartbeat test covers only a non-production descriptor.

START-row-before-file recovery has no reconstruction path, so a crash can cause a later stage to reset the global
fourteen-day wall. The real RUNNING-crash path records CRASH/PAUSED_REVIEW without a terminal, while RESUME refuses
exactly that state; its test manually invokes normal sealing to manufacture the missing terminal. The durable
terminal row still binds only the database-free core, leaving the post-terminal database/full-manifest identity in
coordinately editable files. NON_ARMED database insertion errors are swallowed, and initialized-tree bytes are not
remeasured before network authority.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make an eleventh correction only in the same seven files. It must retain raw/attempt/lineage
atomicity without holding the database transaction across streaming; prove durable production-bound heartbeat
visibility before persistence completes; reconstruct earliest START across the row/file crash boundary; make the
real crash path and prior-terminal/new-permit RESUME compose; durably bind the post-terminal database/full manifest
against coordinated rewrite; make public NON_ARMED fail closed and remeasure initialized trees; and add the exact
public adversarial tests while preserving the corrected readiness, lineage, replay, and migration work.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - eleventh controller correction rejected (2026-08-10)

Jr published the prior review at `af3c178`; `HEAD == origin/main`. Sr's eleventh correction changes all seven
authorized files and is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff,
repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git action.

Physical streaming now precedes the short atomic catalog/attempt/lineage transaction, and the new production-bound
test observes a durable heartbeat from an independent connection before persistence completes. The real crash path
creates a terminal, a missing first-start file is reconstructed, NON_ARMED fails closed, and a schema seal row binds
the terminal files. Those corrections are retained.

The seal row records a pre-seal database hash and then changes that database; authentication explicitly does not
recompute a live database digest. The existing unrelated-table tamper test therefore has no implemented reason to
fail. RESUME transitions the new stage and acquires its lease before validating the prior terminal/permit, leaking
authority on rejection, and it never authenticates the prior directory/seal. First-start reconstruction selects the
globally earliest START without plan scoping or event-chain/policy/permit authentication. ARMED remeasurement allows
database growth and substitutes the recorded pre-insert size for live database bytes.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a twelfth correction only in the same seven files. It must use a recomputable post-seal
database authority that detects unrelated mutation without self-reference; fully authenticate RESUME prerequisites
before an atomic no-residue authorization; authenticate the fixed plan's exact earliest START; and measure/digest
the actual post-prepare database and complete initialized trees before network authority. Tests must cover live DB
tamper, torn prior seal, bad-then-good RESUME state/lease residue, foreign/invalid START rows, and live tree changes
while preserving the corrected heartbeat, atomicity, crash, NON_ARMED, readiness, lineage, and migration paths.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - twelfth controller correction rejected (2026-08-10)

Jr published the prior review at `30af75e`; `HEAD == origin/main`. Sr's twelfth correction changes all seven
authorized files and is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff,
repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git action.

The new live digest detects added tables/rows, prior-directory authentication precedes RESUME authorization,
first-start recovery is plan-scoped and reconciles durable rows, and ARMED uses a live post-insert byte measure.
Those corrections are retained. The digest still omits schema SQL, indexes, triggers, and views. RESUME remains
three separately committed mutations; its cleanup can delete a legitimate foreign lease. Final ARMED bytes live
only in rewritable JSON and intentionally disagree with the immutable schema row. START recovery recomputes payload
hashes but not event hashes.

Two decisive tests also cannot execute their promised paths: the public ARMED test omits the newly mandatory
database path, and the torn-seal test performs an uncaught trigger-prohibited DELETE. There is no foreign-lease,
atomic crash-boundary, schema/index/trigger tamper, coordinated ARMED rewrite, or invalid-event-hash coverage.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a thirteenth correction only in the same seven files. It must seal canonical governed
schema plus deterministic keyed rows; grant stage/lease/RESUME in one transaction without touching foreign leases;
immutably bind the final live ARMED baseline; recompute START event hashes; repair the contradictory tests; and add
the exact missing adversarial cases while preserving the corrected heartbeat, persistence, crash, prior-seal,
NON_ARMED, readiness, lineage, and migration work.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - thirteenth controller correction rejected (2026-08-10)

Jr published the prior review at `e4646ba`; `HEAD == origin/main`. Sr's thirteenth correction changes all seven
authorized files and is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff,
repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git action.

The live digest now covers normal schema SQL and keyed rows; stage/lease/RESUME grant uses one transaction and
preserves foreign leases; START recovery recomputes event hashes; immutable schema authority cross-binds ARMED; and
the contradictory tests are repaired. Those corrections are retained.

The digest nevertheless excludes the complete terminal-seal and prepare-baseline table definitions plus every
attached trigger/index. Dropping their immutability triggers and rewriting their excluded rows can forge both stage
and ARMED authority without changing the digest. Its SQL-substring exclusion can also hide a newly added unrelated
schema object. Tests cover only a normal index drop and omit authority-trigger/row forgery, substring-evasive schema,
and injected rollback boundaries inside the atomic grant.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a fourteenth correction only in the same seven files. It must hash all static authority-
table schema/index/trigger definitions, exclude only unavoidable self-referential row values using exact rules,
remove substring exclusions, and add authority-trigger/row forgery plus each atomic grant failure-boundary test.
All corrected digest, transaction, START, ARMED, heartbeat, persistence, crash, prior-seal, NON_ARMED, readiness,
lineage, and migration behavior must remain intact.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - fourteenth controller correction rejected (2026-08-10)

Jr published the prior review at `949d9fd`; `HEAD == origin/main`. Sr's fourteenth correction changes all seven
authorized files and is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK. Targeted ruff,
repository control, and `git diff --check` pass. Sol ran no pytest, migration, RPC, production-data, or Git action.

Authority schema/index/trigger definitions are now hashed without substring exclusions, and real injected failures
prove rollback after each stage/lease/RESUME transaction boundary while preserving foreign leases. Those corrections
are retained.

The expected digest still lives only in completely omitted authority rows. After dropping a now-hashed immutability
trigger, an attacker can compute the changed digest, rewrite the excluded row and corresponding files consistently,
and pass authentication. The new tests do not execute that coordinated forgery: one leaves stale fields and the
other writes an intentionally wrong zero digest.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files. After
publication, Sr may make a fifteenth correction only in the same seven files. It must pin the required authority
schema independently of the rows it protects and add exact coordinated trigger-drop/recomputed-row/file forgery
tests for terminal seal and ARMED baseline. All corrected digest, transaction, rollback, START, ARMED, heartbeat,
persistence, crash, prior-seal, NON_ARMED, readiness, lineage, and migration behavior must remain intact.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source acceptance - fifteenth controller correction accepted (2026-08-10)

Jr published the prior review at `c3290d5`; `HEAD == origin/main`. Sol accepts Sr's fifteenth correction in
all seven authorized files for Jr integration and offline testing. Reviewed SHA-256 values are recorded in
CURRENT_TASK; targeted ruff, repository control, and `git diff --check` pass. Sol ran no pytest, migration,
RPC, or production mutation.

The source-pinned authority-schema identity is now checked before trusting terminal-seal or prepare-baseline
rows, so a rewritten authority row cannot bless removal or alteration of its own protection. Coordinated
trigger-drop/recomputed-row/file forgery is covered for both the terminal seal and the ARMED baseline, including
authority-row deletion/reinsertion where constraints allow, without placeholder digests. The corrected digest,
transaction, rollback, START, ARMED, heartbeat, persistence, crash, prior-seal, NON_ARMED, readiness, lineage,
and migration behavior remain intact.

Jr must first commit/push only this two-record acceptance, excluding the uncommitted drop and unrelated files.
After publication, Jr verifies the seven hashes, integrates the unchanged drop, runs the focused suite and
controls, applies migration 0021 to the offline database, records evidence, and commits/pushes only the accepted
files and aligned records. Live readiness, production preparation/RPC/stages, coverage, publication, downstream,
PAPER, LIVE, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Sol source review - second controller correction rejected (2026-08-10)

Jr published the prior rejection at `343e931`; `HEAD == origin/main`. Sr's second correction touches all
seven authorized files but is rejected. Exact hashes and full evidence are recorded in CURRENT_TASK.
Targeted ruff passes, repository control passes, and `git diff --check` is clean. Sol ran no pytest,
migration, RPC, production-data, or Git operation.

The new readiness smoke calls `process_one`, but it is not the frozen workload: the CLI accepts caller pool
births/limits and optional pin enforcement; the executor initializes all roots of a non-production plan,
processes only a small partial budget, and reuses consumed state across rungs. It does not select the lowest
128 production root geometries, retain/authenticate all attempts/raw/replay/splits/headers, compute the four-
group byte/storage model, or seal/authenticate the full database/raw inventory. Production prepare still
inserts only caller-influenced policy. Stage-run opens mutable state before its lock, accepts caller rung/step
values, never attaches/authenticates the existing production plan, renews/checkpoints only after blocking work,
and converts its inevitable engine-phase failure into a successful CLI result. Attempt attribution, registry-
birth credit, controller clocks/crash/storage/drain, and semantic stage authentication remain incomplete.
Migration tests are unchanged and production coverage has only eight smoke tests.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files.
After publication, Sr may correct only the same seven files. The third correction must implement the exact
canonical readiness authority and independent complete rung execution, immutable zero-network evidence
authentication, full-root offline production initialization, existing-plan stage attachment with concurrent
renewal/checkpoints/stops, atomic real-attempt and birth-clamped credit authority, exact controller/sealing/
crash semantics, remaining composite migration constraints, and the complete decisive public-path test matrix.
Caller pool/hash/policy/rung/limit/PTB substitution and private-state test mutation are prohibited.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Sol source review - first controller correction rejected (2026-08-09)

Jr published the prior rejection at `7450afc`; `HEAD == origin/main`. Sr's first correction stays within
the authorized source boundary, but it is rejected. Exact reviewed hashes and full evidence are recorded
in CURRENT_TASK. Targeted ruff passes, repository control passes, and `git diff --check` is clean. Sol ran
no pytest, migration, RPC, production-data, or Git operation.

The correction is still scaffolding rather than ADR-0015 section 9.11 execution. Readiness accepts caller
domain IDs and reports eligible rungs after constructing/closing engines without a network attempt, root,
split, header, raw replay, or authenticated evidence. Its authenticator checks only an identity in mutable
JSON. Production preparation trusts that JSON, synthesizes missing hashes, accepts caller source hashes,
and neither initializes/authenticates roots nor seals READY. `stage-run` creates a RUNNING stage/lease and
exits without an engine, acquisition, renewal, checkpoint, drain, or terminal. Engine attempts are not
attributed; credit is not registry-birth authoritative; clocks/storage/crash/sealing remain incomplete;
the stage authenticator accepts a wrong nonempty report hash and performs no complete semantic replay; and
migration composites still allow policy/permit/ordinal, domain/leaf, and terminal/checkpoint mismatches.
The twelve production and six migration tests do not execute the frozen decisive paths.

Jr must first commit/push only this two-record review, excluding the uncommitted drop and unrelated files.
After publication, Sr may correct only the same seven files. The correction must deliver the actual public
readiness workload/evidence/replay and selection, authenticated offline full-root production preparation,
complete gated stage controller with per-boundary durable capability/attempt/credit authority, exact clocks/
storage/drain/crash/sealing, immutable semantic authentication, composite migration authority, and the full
decisive public-path/populated-upgrade test matrix. Confirmation must precede mutation; caller domain/PTB/
hash assertions, lease theft, and private substitute semantics are prohibited.

Sr does not run tests/migrations, edit other files/records, use RPC/network, touch production/evidence data,
or use Git, and stops for Sol source review. Jr integration and all live readiness, production initialization,
RPC/stages, coverage, publication, downstream, PAPER, LIVE, and next-ticket work remain unauthorized. Next
ticket remains `NONE`.

## Latest control-plane state (2026-08-17)

The authoritative current decision is the detailed section titled
`Sol integration review - fifteenth controller acceptance withdrawn (2026-08-17)`. The fifteenth source
acceptance is withdrawn and migration 0021 was not applied. Jr Dev - Hermes is the next actor only to publish
the aligned two-record review. The sixteenth correction becomes available to Sr Dev - Grok Build at the recorded
Grok 4.6 Max escalation tier only after publication and must stop for fresh Sol source review. Jr
reintegration/testing, live readiness, production preparation/RPC/stages, coverage, publication, downstream,
PAPER, LIVE, harmonic-model, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Sol source review - sixteenth controller correction rejected (2026-08-17)

Jr published the withdrawn fifteenth acceptance, integration rejection, and sixteenth-correction
authorization at pushed commit `8f3c382`; `HEAD == origin/main`. Sr delivered the sixteenth
correction inside the same seven authorized files. Reviewed SHA-256 values are:

- `src/cryptofactors/acquisition/uniswap_v2_pair_events_v2_engine.py`:
  `7953c8b6b7590391ca17d44bbdfe24715ac1fba5d70b419a00c802538504e668`;
- `src/cryptofactors/acquisition/uniswap_v2_pair_events_v2_production.py`:
  `1e781daceb411e978b9cde261453413aa8d60190c9a4e0fccce578641cec8200`;
- `scripts/research/run_uniswap_v2_pair_events_v2_production.py`:
  `4a9aeb260bf085fb55d15f18432ab3c196ecce9aba6407a636b4cbc898fae06c`;
- `sql/migrations/0021_uniswap_v2_pair_event_v2_production_control.sql`:
  `5fb6bdbdc6e9d05cdb7ddba5bff1b9bf384ac92c419f16a4def039aacf764d52`;
- `tests/acquisition/test_uniswap_v2_pair_events_v2_engine.py`:
  `a811dc414cfe3bf006571a0e48171626ff654478cb6e49bfed7a285f28ac47d0`;
- `tests/acquisition/test_uniswap_v2_pair_events_v2_production.py`:
  `402ed6d0c8d0287804ffb85a147e9d186dbda7906b2a3f2b01c0d8aa7969f5ef`; and
- `tests/acquisition/test_uniswap_v2_pair_events_v2_migration_0021.py`:
  `a0a3fe0d1a4909fe328483722e609c79666e71951ca3209a36d48f164aeed957`.

The correction closes important integration failures. Stable acquisition identity now reaches deferred
catalog registration; raw/attempt/lineage writes share the short owner transaction; the public controller
attaches READY, authenticates chain, runs, and drains only tracked started work; malformed readiness is
normalized; the CLI rejects root symlinks before resolution; and natural production queries select the
nonredundant named indexes. Acceptance is still blocked:

1. The startup replay test cleans the journal before its second engine starts, so it does not exercise
   commit-before-cleanup. Failure replay still uses direct persistence. Source deletes spool before journal,
   so a cleanup-boundary crash can reinterpret a committed success as failure, and malformed production
   journal fields may be silently skipped.
2. Existing attempt replay compares only five identity fields and silently accepts divergence in role,
   domain/block, timestamps, status, byte counts, outcome, acquisition/raw IDs, and created time. Existing
   lineage is not checked against its recomputed deterministic ID. Full acquisition/attempt/lineage
   divergence and retained-journal refusal are untested.
3. Seal and baseline database bytes/SHA are measured before inserting their authority rows. The insert
   changes the final database, while authentication compares SHA only between MANIFEST and the seal rather
   than to the live file. Post-COMMIT checkpoint failures then enter handlers that attempt ROLLBACK, risking
   a masked error with committed partial state. Immediate immutable-read final authority is not established.
4. Recursive controller/raw/spool inventory skips symlinks, and authentication resolves listed candidates
   before checking `is_symlink` while skipping symlinks from orphan enumeration. Root/nested symlinks remain
   accepted outside the stage directory.
5. The foreign-plan test creates no foreign policy/permit/stage/START graph. There is no real failed startup
   replay, committed cleanup-fault replay, full-row divergence matrix, or live final database byte/digest
   assertion.

Syntax compilation, targeted ruff, repository control, and `git diff --check` pass. The seven-file source
boundary is exact. Sol ran no pytest, migration, RPC, production-data mutation, or Git action.

### Authorized seventeenth correction - same seven files only

Jr Dev - Hermes must first commit and push only this aligned review in
`docs/handoff/CURRENT_TASK.md` and `tickets/DEX-003.md`, excluding the uncommitted seven-file drop,
`opencode.json`, both untracked research files, logs, databases, and every unrelated path. After publication,
Sr Dev - Grok Build remains at Grok 4.6 Max and may correct only the same seven files:

- Implement exact fail-closed public startup recovery for committed/not-committed/partial-cleanup success and
  failure states. Authenticate every stable journal-bound acquisition/catalog/attempt/lineage field, retain
  unresolved journals, and persist every replay authority that must not be regenerated.
- Make terminal and ARMED row/COMMIT/checkpoint/file publication resumable. Require immediate clean immutable
  authentication with no WAL/SHM dependency; remeasure and bind exact final live database bytes and the
  non-self-referential governed digest after authority insertion; never call a pre-insert physical SHA final.
- Reject root and nested symlinks before resolution during measurement and authentication. Preserve all corrected
  lifecycle, index, readiness, atomicity, schema-pin, and forgery behavior.
- Add decisive public tests for both startup outcomes and all cleanup boundaries, every immutable row-field
  divergence, a genuine foreign START graph, final live seal/baseline byte/digest agreement and tear recovery,
  and recursive symlink refusal. Direct persistence, empty replay, swallowed error, conditional assertion, and
  earlier-guard substitutes are prohibited.

Sr does not run tests or migrations, edit repository records or unrelated files, use RPC/network, touch the
offline production database or evidence, or perform Git actions. Sr stops for fresh Sol source review with new
hashes. Jr reintegration/testing and migration 0021 remain unauthorized. All live readiness, production prepare/
RPC/stages, coverage, publication, downstream, PAPER, LIVE, harmonic-model, and next-ticket work remain
unauthorized. Next ticket remains `NONE`.

## Latest control-plane state (2026-08-17)

The authoritative current decision is the immediately preceding section,
`Sol source review - sixteenth controller correction rejected (2026-08-17)`. Jr Dev - Hermes is the next actor
only to publish this aligned two-record review. The seventeenth correction becomes available to Sr Dev - Grok
Build at Grok 4.6 Max only after publication and must stop for fresh Sol source review. Jr reintegration/testing,
migration 0021, live readiness, production preparation/RPC/stages, coverage, publication, downstream, PAPER,
LIVE, harmonic-model, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Sol source review - seventeenth controller correction rejected (2026-08-17)

Jr published the sixteenth-correction rejection and seventeenth-correction authorization at pushed commit
`395c6bf`; `HEAD == origin/main`. Sr delivered the seventeenth correction inside the same seven authorized
files. Reviewed SHA-256 values are:

- `src/cryptofactors/acquisition/uniswap_v2_pair_events_v2_engine.py`:
  `941bd12122c7a1c656edee6fd37623113bfadff1ee8049206569d2845b0299bb`;
- `src/cryptofactors/acquisition/uniswap_v2_pair_events_v2_production.py`:
  `30834af1bf5baeff6a2d080941b78edece30daa06c600ab99d0ae28cc1bc82f4`;
- `scripts/research/run_uniswap_v2_pair_events_v2_production.py`:
  `4a9aeb260bf085fb55d15f18432ab3c196ecce9aba6407a636b4cbc898fae06c`;
- `sql/migrations/0021_uniswap_v2_pair_event_v2_production_control.sql`:
  `11747236f25752733f825ef06566a2b806f559026d1643ec40ee89b6600f6d7c`;
- `tests/acquisition/test_uniswap_v2_pair_events_v2_engine.py`:
  `0354bfd4e740a4384ff9a84b642603d26f8bcd22f0cf6f74af1017c178a41efc`;
- `tests/acquisition/test_uniswap_v2_pair_events_v2_production.py`:
  `5436935ec44126896f074f9b8db9023193673f2cc34eb698e2e864cff986d685`; and
- `tests/acquisition/test_uniswap_v2_pair_events_v2_migration_0021.py`:
  `ee95887972491df60fe63abcd5db8311b18ab042f21dd716e908373b17a34ae5`.

The dedicated production recovery path, fail-closed recognized authority parsing, nested-symlink rejection,
WAL flush propagation, and natural index behavior are retained. Acceptance remains blocked:

1. Exact replay snapshots are written after database COMMIT, are not durably fsynced, may silently be skipped,
   and are optional during authentication. Catalog, attempt, and lineage fields are still not fully compared to
   journal authority; several expected lineage values are taken from the row being authenticated.
2. Cleanup now deletes journal before spool, but the between-unlink crash leaves an orphan spool that startup
   only counts against capacity. The new partial test constructs the opposite ordering and cannot prove the
   implemented crash boundary.
3. Seal SHA/bytes are measured after provisional insert but before a final database UPDATE/flush. The stored SHA
   is therefore still pre-final, and authentication never hashes the live database. `file_identity_final` has no
   retry/recovery protocol for torn row/checkpoint/MANIFEST/TERMINAL boundaries; no tear injection test exists.
4. Stage recursive inventory rejects nested symlinks but resolves a policy-bound root before checking whether the
   root itself is a symlink. Only the CLI root helper and nested raw-file case are tested.
5. The promised divergence test mutates only status while replaying a journal without the optional snapshot, so
   its mutation is not detectable. Its setup helper broadly swallows authority-construction failures. Seal and
   baseline delete/reinsert tests omit mandatory `file_identity_final` and fail at NOT NULL before their intended
   assertions. The foreign START is directly appended to PREPARED rather than built through public lifecycle.

Syntax compilation, targeted ruff, repository control, and `git diff --check` pass. The seven-file source
boundary is exact. Sol ran no pytest, migration, RPC, production-data mutation, or Git action.

### Authorized eighteenth correction - same seven files only

Jr Dev - Hermes must first commit and push only this aligned review in
`docs/handoff/CURRENT_TASK.md` and `tickets/DEX-003.md`, excluding the uncommitted seven-file drop,
`opencode.json`, both untracked research files, logs, databases, and every unrelated path. After publication,
Sr Dev - Grok Build remains at Grok 4.6 Max and may correct only the same seven files:

- Durably freeze the complete deterministic success/failure replay authority before its database commit; compare
  every catalog/attempt/lineage field and fail closed with evidence retained on absence or divergence.
- Implement the actual cleanup-order state machine without orphan capacity leaks, covering every unlink boundary.
- Make final database facts describe the state after the last mutation and recompute every claimed fact during
  immutable authentication. Add idempotent recovery across each row/COMMIT/checkpoint/file boundary.
- Reject root symlinks before resolution, repair broad-catching/invalid fixtures and schema inserts, and add the
  full real field-divergence, cleanup-order, public foreign START, live SHA/bytes/digest, root-symlink, and tear-
  recovery test matrix. Preserve all already-corrected behavior.

Sr does not run tests or migrations, edit repository records or unrelated files, use RPC/network, touch the
offline production database or evidence, or perform Git actions. Sr stops for fresh Sol source review with new
hashes. Jr reintegration/testing and migration 0021 remain unauthorized. All live readiness, production prepare/
RPC/stages, coverage, publication, downstream, PAPER, LIVE, harmonic-model, and next-ticket work remain
unauthorized. Next ticket remains `NONE`.

## Latest control-plane state (2026-08-17)

The authoritative current decision is the immediately preceding section,
`Sol source review - seventeenth controller correction rejected (2026-08-17)`. Jr Dev - Hermes is the next actor
only to publish this aligned two-record review. The eighteenth correction becomes available to Sr Dev - Grok
Build at Grok 4.6 Max only after publication and must stop for fresh Sol source review. Jr reintegration/testing,
migration 0021, live readiness, production preparation/RPC/stages, coverage, publication, downstream, PAPER,
LIVE, harmonic-model, and next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Sol source review - eighteenth controller correction rejected (2026-08-17)

Jr published the prior review at pushed commit `8ef359f`; `HEAD == origin/main`. Sr's eighteenth correction stayed
inside the seven authorized files. Reviewed SHA-256 values are:

- engine: `d344c8abea5c8bf6a206d7aaf6b48ea1795888ffee9b4ac5de32a9ae55128018`;
- production controller: `967beccabce8892f0eeade86ff7269bd423b7142dc1d3259cb35eba43214ab0c`;
- CLI: `4a9aeb260bf085fb55d15f18432ab3c196ecce9aba6407a636b4cbc898fae06c`;
- migration 0021: `5512f7818f673208172768e9f732f2402e9d11e9350d0c5177fc67bb01473c4f`;
- engine tests: `ff663e014be041f128629493dbb88eecee48a98f5b9d7fb844b6cdc5d5001d52`;
- production tests: `8ce2028965705ca3e8409ef105f4786ea3e904f17b950f1bd568f0e559dbd484`; and
- migration tests: `ee95887972491df60fe63abcd5db8311b18ab042f21dd716e908373b17a34ae5`.

Durable pre-COMMIT snapshots, declared-column committed authentication, cleanup intents for the real unlink order,
unattributable-orphan refusal, root-symlink rejection, the public foreign START graph, and live external
MANIFEST/TERMINAL hashing are retained. Acceptance remains blocked:

1. Snapshot-before-COMMIT recovery reads `ended_at`, `created_at`, and `status` from the snapshot top level even
   though they are nested under `attempt`, then regenerates the authority if the snapshot is durable but the
   transaction is absent. No success/failure injection covers that exact boundary.
2. The immutable terminal seal stores content-digest/zero sentinels in `database_sha256`, `database_bytes`, and
   `full_manifest_hash`, marks `file_identity_final = 1`, and authentication ignores those selected fields. The
   live test also omits them. Retaining misleading final fields is silent partial authority.
3. Terminal recovery requires the terminal row and therefore cannot recover stage/lease/checkpoint/CORE-to-row
   boundaries. Tests mutate files only after a completed seal rather than injecting each real publication tear.
4. The prepare baseline stores pre-insert database/tree bytes and marks them final; later remeasurement updates
   only PREPARE_OUTCOME. Authentication selects but never compares the immutable row's two byte fields, and no
   public prepare recovery or injected tear matrix exists.
5. The divergence test covers twelve fields rather than every declared catalog/attempt/lineage column and omits
   pre-COMMIT replay, retained seal fields, and baseline fields.

Syntax compilation, targeted ruff, repository control, and `git diff --check` pass. The boundary is exact. Sol
ran no pytest, migration, RPC, production-data mutation, or Git action.

### Authorized nineteenth correction - same seven files only

Jr must first publish only this aligned two-record review, excluding the source drop and unrelated paths. After
publication Grok 4.6 Max may change only the same seven files to:

- replay every generated success/failure field solely from the durable pre-COMMIT snapshot, with injected success
  and failure recovery and complete declared-column comparison;
- remove impossible self-referential physical fields from immutable rows or make every retained named field exact,
  with no sentinel-final state and authentication of every retained seal/baseline value;
- provide idempotent terminal and ARMED recovery across every actual mutation/COMMIT/checkpoint/file boundary,
  tested by injection rather than post-completion deletion; and
- bind and authenticate final ARMED database/tree facts, complete all replay/seal/baseline divergence matrices,
  and preserve the accepted cleanup, symlink, lifecycle, schema, atomicity, and external-file behavior.

Sr does not run tests or migrations, edit records/unrelated files, use network/RPC, touch production evidence, or
perform Git actions, and stops for fresh Sol review. Jr integration/testing and migration 0021 remain unauthorized.
All live readiness, preparation/RPC/stages, coverage, publication, downstream, PAPER, LIVE, harmonic-model, and
next-ticket work remain unauthorized. Next ticket remains `NONE`.

## Latest control-plane state (2026-08-17)

The authoritative decision is the immediately preceding eighteenth-correction rejection. Jr Dev - Hermes is the
next actor only to publish this aligned review. The nineteenth correction is available to Grok 4.6 Max only after
publication and must stop for fresh Sol source review. No integration, migration, live work, harmonic-model work,
or next ticket is authorized. Next ticket remains `NONE`.

## Sol source review - nineteenth controller correction rejected (2026-08-17)

Jr published the prior review at pushed commit `880c509`; `HEAD == origin/main`. Sr's nineteenth correction stayed
inside the seven authorized files. Reviewed SHA-256 values are:

- engine: `d19afce7f7cdad15e07bc966d2813f3b96af550134c2919933d592c5deb9327c`;
- production controller: `f0d04d0ca2572a88757fdb9ef02c2f50915561e054e12563443d730faa70e15b`;
- CLI: `4a9aeb260bf085fb55d15f18432ab3c196ecce9aba6407a636b4cbc898fae06c`;
- migration 0021: `be950e9700a95c828c0c5a2aee8e391581316a0aa7eff427c6c31f5c921fd13f`;
- engine tests: `129da81c4e9191f36850e5a0e6bab1db666a0461a5595ec3e67b74f490b75726`;
- production tests: `63931f0c42210c0b2f462284276a425a407c6e451eed204b143e7fa689106b01`; and
- migration tests: `66d9a86ce409601380b29cf9c936fe3013ed4f07a2a80261bdc325a15bf5a308`.

Direct frozen-row replay, the complete named live-row matrix, honest non-self-referential seal/baseline schema,
external live-file measurements, PUBLICATION/ARMED recovery entry points, symlink refusal, and nominal boundary
injection are retained. Acceptance remains blocked:

1. All-rows-absent recovery checks only snapshot shape/columns before insertion. It does not bind snapshot values
   and deterministic attempt/lineage/cross-row identities to journal `production_authority`, so a divergent frozen
   snapshot is promoted and cleaned up rather than refused.
2. Successful frozen recovery derives the claimed SHA from `raw_object_id` but never hashes retained bytes or
   authenticates a preexisting/new `raw_object` row. Corrupt evidence can be registered as successful before the
   journal and spool are deleted.
3. Tests cover unmodified missing-transaction replay and post-commit row divergence, but no absent-row snapshot
   divergence, cross-row inconsistency, raw corruption, or divergent existing raw-object case with zero partial
   mutation and retained evidence.
4. Terminal/seal/baseline row hooks run after their COMMIT and `flush_to_main`, not in the required
   COMMIT-before-checkpoint window; swallowed checkpoint errors also remain. Nominal boundary names do not prove
   the actual durability tears.
5. Terminal recovery trusts PUBLICATION before mutation, directly deletes any plan lease, and swallows deletion
   errors. Divergent intent or a newer/foreign lease can be mutated before later authentication refuses.

Syntax compilation, targeted ruff, repository control, and `git diff --check` pass. The boundary is exact. Sol
ran no pytest, migration, RPC, production-data mutation, or Git action.

### Authorized twentieth correction - same seven files only

Jr must first publish only this aligned two-record review, excluding the source drop and unrelated paths. After
publication Grok 4.6 Max may change only the same seven files to:

- fully authenticate an all-rows-absent snapshot and every deterministic/cross-row relation before insertion;
- hash and authenticate retained raw bytes plus every existing/new raw-object field before successful replay;
- test absent-row snapshot/raw/raw-object divergence with zero partial rows and retained evidence; and
- inject the actual COMMIT-before-flush/checkpoint windows, recover/refuse checkpoint failure explicitly, and
  authenticate PUBLICATION plus lease ownership before any recovery mutation without swallowed errors.

The honest seal/baseline schema, external live-file facts, idempotent file recovery, cleanup, symlink, lifecycle,
schema-pin, and atomicity corrections must remain. Sr performs no tests, migrations, records, Git, RPC/network, or
production-evidence work and stops for fresh Sol review. Jr integration and migration 0021 remain unauthorized;
all live, downstream, harmonic-model, and next-ticket work remains unauthorized. Next ticket remains `NONE`.

## Latest control-plane state (2026-08-17)

The authoritative decision is the immediately preceding nineteenth-correction rejection. Jr Dev - Hermes is the
next actor only to publish this aligned review. The twentieth correction is available to Grok 4.6 Max only after
publication and must stop for fresh Sol source review. No integration, migration, live work, harmonic-model work,
or next ticket is authorized. Next ticket remains `NONE`.

## Sol source review - twentieth controller correction rejected (2026-08-17)

Jr published the prior review at pushed commit `25adcfb`; `HEAD == origin/main`. Sr's twentieth correction stayed
inside the seven authorized files. Reviewed SHA-256 values are:

- engine: `6b3b49b33268a319215ac56822f3416b0525d258025cfa1edbfa129559259ece`;
- production controller: `4c95aa8bf6896cc8fbadc05699f36552ccd0694f9da658352e3f3cc2f3c979a9`;
- CLI: `4a9aeb260bf085fb55d15f18432ab3c196ecce9aba6407a636b4cbc898fae06c`;
- migration 0021: `be950e9700a95c828c0c5a2aee8e391581316a0aa7eff427c6c31f5c921fd13f`;
- engine tests: `5057613967cfc0f5eedda0a5a01c88cf907c348e44839d59ddc8e799c2103aee`;
- production tests: `a0ebeaf1cbbe8446f27cf928790fb0dc86fefd0bf0756d01d9fd9e2e6f5e0abf`; and
- migration tests: `66d9a86ce409601380b29cf9c936fe3013ed4f07a2a80261bdc325a15bf5a308`.

Journal-bound identity validation, no-follow raw hashing, publication-receipt verification, representative
absent-row refusal tests, real post-COMMIT hooks, pre-mutation PUBLICATION validation, and foreign-stage lease
retention are retained. Acceptance remains blocked:

1. A `SUCCEEDED` frozen snapshot may still carry `(NULL, NULL)` attempt acquisition/raw links while catalog and
   lineage are successful; validation accepts and commits it. No test covers this branch.
2. The raw-object row is not frozen and is synthesized incorrectly: recovery writes content status `SUCCEEDED`,
   while uninterrupted public registration writes the default content status `ACQUIRED`. Exact resumability and
   provenance therefore diverge.
3. Recovery requires every acquisition-specific raw-object field to equal the current acquisition, incorrectly
   rejecting legitimate content-address reuse whose content row was created by an earlier request/provider. No
   valid reuse test exists.
4. Absent-row authentication/tests do not cover every journal-derived snapshot field; request, metadata,
   acquired time, request bytes, status, and outcome can remain schema-valid yet divergent.
5. The checkpoint-failure test is masked by an earlier boundary hook, swallowed checkpoint exceptions remain,
   and same-stage newer lease ownership is neither bound nor tested before deletion.

Syntax compilation, targeted ruff, repository control, and `git diff --check` pass. The boundary is exact. Sol
ran no pytest, migration, RPC, production-data mutation, or Git action.

### Authorized twenty-first correction - same seven files only

Jr must first publish only this aligned two-record review, excluding source and unrelated paths. After publication
Grok 4.6 Max may change only the same seven files to:

- freeze and exactly replay complete raw-object authority, distinguishing new content from valid existing reuse;
- require exact success/failure attempt/catalog/lineage links and validate every journal-derived field before write;
- test uninterrupted-versus-recovered equality for every row, NULL-pair refusal, full absent-row divergence,
  valid reuse, raw corruption, and divergent existing content with zero partial mutation; and
- exercise genuine checkpoint failure and bind/delete only the exact intent-time lease, refusing a newer
  same-stage owner.

All corrected seal/baseline, live-file, recovery, cleanup, symlink, lifecycle, and source-pin behavior must remain.
Sr performs no tests, migrations, records, Git, RPC/network, or production-evidence work and stops for fresh Sol
review. Jr integration and migration 0021 remain unauthorized; all live, downstream, harmonic-model, and next-
ticket work remains unauthorized. Next ticket remains `NONE`.

## Latest control-plane state (2026-08-17)

The authoritative decision is the immediately preceding twentieth-correction rejection. Jr Dev - Hermes is the
next actor only to publish this aligned review. The twenty-first correction is available to Grok 4.6 Max only
after publication and must stop for fresh Sol source review. No integration, migration, live work, harmonic-model
work, or next ticket is authorized. Next ticket remains `NONE`.
