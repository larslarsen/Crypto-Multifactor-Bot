# EXP-002 — MOM-TS-01 Perpetual Execution and Results

**Priority:** P0
**Status:** READY
**Dependencies:** PORT-002 (ACCEPTED)
**Layer:** experiments
**Architecture:** Research execution script and runner wiring.

## Objective

Update the MOM-TS-01 runner to utilize the newly built `PerpetualSimulator` (PORT-002) and write the execution script that will run the registered experiments (`EXP-2026-019` and `EXP-2026-020`) to produce the final research artifacts.

## Scope

1. **Runner Wiring:**
   - Update `src/cryptofactors/experiments/momts_runner.py`.
   - Change `run_30_7` and `run_90_7` to call `run_perpetual_experiment` instead of `run_experiment`.
   - Update `MOMTSRunnerResult` to wrap `PerpetualSimulationResult` and expose the required metrics (liquidation_count, long_return, short_return, etc.).
   - Ensure the `build_momts_30_7_bundle` and `build_momts_90_7_bundle` reflect the `portfolio_cell = "perp_ls"` and `portfolio_version = "perp_ls_v1"`.

2. **Execution Script:**
   - Create `scripts/run_momts_experiments.py`.
   - This script should initialize the `AsOfStore`, load the BitMEX funding data (via `BitMEXFundingProvider`), initialize the `MOMTSRunner`, and execute `run_30_7` and `run_90_7` over a representative date range (e.g., weekly decision times).
   - The script should print or save the final performance metrics (Net Return, Liquidations, L/S Attribution, Turnover) to stdout or a results file, fulfilling the preregistered metric requirements.

## Deliverables

- Updated `src/cryptofactors/experiments/momts_runner.py`
- Updated `tests/experiments/test_momts_runner.py`
- New `scripts/run_momts_experiments.py`

## Acceptance (Jr)

1. `.venv/bin/python -m pytest tests/experiments/ -q --tb=short`
2. `.venv/bin/python -m ruff check src/cryptofactors/experiments scripts/`
3. `.venv/bin/python -m mypy --no-error-summary src/cryptofactors/experiments scripts/`
4. `python3 scripts/check_repo_control.py`

## Phased Ownership

- Sr Dev (Strong Model): logic and production source. Stop for reviewer.
- Jr Dev (Weak Model): tests, gates, records, Git, commit, push after approval.

## Stop Condition

After Sr Dev implementation: Set state to AWAITING_REVIEW, wait for Lead Quant review, Next ticket authorized: NONE.
