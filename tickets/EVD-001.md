# EVD-001 — Operational Evidence Registry

**Priority:** P1 after CAT-001  
**Dependencies:** CAT-001  
**Layer:** evidence
**Status:** AWAITING_REVIEW - JR INTEGRATION COMPLETE

**Current authorization:** Jr integration under
`docs/reviews/REVIEW-0055_EVD-001_SOURCE_APPROVED_JR_AUTHORIZED.md`.
The experiment-link deliverable is deferred until the experiments layer defines fingerprint
identity; EVD-001 must not invent that contract.

## Objective

Implement hypothesis registration, evidence registration/linking, evidence snapshots, and append-only decisions using migration 0002.

## Deliverables

- typed repository/service API;
- CLI commands to list/show/register/export;
- deterministic content hashes;
- immutable hypothesis versions;
- append-only decisions;
- generated Markdown/JSON current-state report;
- seed import of `research/evidence/hypotheses.yaml`;
- no performance score aggregation.

## Scope clarification

Use the existing migration-0002 tables and canonical hashing contract for hypothesis versions,
evidence, evidence links, snapshots, and append-only decisions. `hypothesis_experiment_link` is
out of scope for this ticket because experiment fingerprint creation/validation is not yet owned
by an implemented experiments layer.

Snapshot/export/seed operations must be deterministic and idempotent. Promotion decisions must
fail closed when a snapshot contains only literature/legacy evidence or promotion evidence has
causal or point-in-time integrity `FAIL`.

## Acceptance commands

1. `PYTHONPATH=src uv run pytest tests/evidence -q --tb=short`
2. `PYTHONPATH=src uv run ruff check src/cryptofactors/evidence src/cryptofactors/cli.py tests/evidence`
3. `PYTHONPATH=src uv run mypy --no-incremental src/cryptofactors/evidence src/cryptofactors/cli.py tests/evidence`
4. `PYTHONPATH=src uv run pytest -q --tb=short`
5. `python3 scripts/check_layer_imports.py`
6. `python3 scripts/check_repo_control.py`

## Phased ownership

- Sr Dev - Grok Build: production source only, then stop for reviewer source inspection.
- Jr Dev - Hermes: begins only after reviewer source approval; owns tests, gates, records, Git,
  commit, and push.

## Invariants

No `SUPPORTED` or `REPLICATED` decision may reference a snapshot containing only literature or legacy results. Evidence with causal or point-in-time integrity `FAIL` cannot support promotion.


## Agent stop condition

After the acceptance commands pass, produce a change report and stop. Do not begin the next ticket.

## Decision

Jr integration complete under `docs/reviews/REVIEW-0055_EVD-001_SOURCE_APPROVED_JR_AUTHORIZED.md`.
All six acceptance commands pass (27 evidence tests, ruff clean, strict mypy clean, 423 full
tests, layer-import check passed, repo control PASS). Change report:
`docs/reviews/EVD-001_CHANGE_REPORT.md`. Next ticket authorized: `NONE`; awaiting reviewer
acceptance. Experiment-link deliverable remains deferred per ticket scope.
