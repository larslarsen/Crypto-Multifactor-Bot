# EVD-001 — Operational Evidence Registry

**Priority:** P1 after CAT-001  
**Dependencies:** CAT-001, experiment identity contract  
**Layer:** evidence
**Status:** AWAITING_REVIEW - READINESS AUDIT COMPLETE

**Current authorization:** Records-only Jr readiness audit under
`docs/reviews/EVD-001_JR_READINESS_TASK.md`. Production implementation is not yet authorized.

## Objective

Implement hypothesis registration, evidence registration/linking, evidence snapshots, and append-only decisions using migration 0002.

## Deliverables

- typed repository/service API;
- CLI commands to list/show/register/export;
- deterministic content hashes;
- immutable hypothesis versions;
- append-only decisions;
- generated Markdown/JSON current-state report;
- seed import of `research/evidence/hypotheses.yaml`;
- no performance score aggregation.

## Invariants

No `SUPPORTED` or `REPLICATED` decision may reference a snapshot containing only literature or legacy results. Evidence with causal or point-in-time integrity `FAIL` cannot support promotion.


## Agent stop condition

After the acceptance commands pass, produce a change report and stop. Do not begin the next ticket.
