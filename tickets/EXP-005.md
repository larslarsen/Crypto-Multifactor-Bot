# EXP-005 — Out-of-Sample / Walk-Forward Validation of Top TSMOM Configs

**Priority:** P0  
**Status:** ACCEPTED  
**Dependencies:** EXP-004 (ACCEPTED), ALLOC-001  
**Layer:** research / factors / paper evidence  
**Architecture:** reuse ALLOC-001 risk, real as-of path, EXP-004 grid runner patterns. **No LIVE.**

## Objective

EXP-004 found several risk-compliant profitable TSMOM cells **in-sample** on one ~12m window (best `tsmom_14_0` +31.12%). Multiple-testing risk is high. Validate whether top configs survive a **holdout or walk-forward** protocol before any paper-promotion or LIVE discussion.

## Scope

1. **Protocol (required):** Choose and document one of:
   - **Holdout split:** e.g. select/rank on first half of available real bars; evaluate frozen top-K on second half only; **or**
   - **Walk-forward:** rolling train/select then test folds with no peek into test fold.
2. **Configs:** At minimum evaluate frozen set including EXP-004 top-3 by in-sample return that had `live_gate_satisfied: true` (`tsmom_14_0`, `tsmom_14_3`, `tsmom_7_0`) **plus** baseline `tsmom_30_7`. Do not re-optimize lookback/skip on the test fold.
3. **Risk:** ALLOC-001 neutrality-preserving enforcement; 0.15 / 1.0 unchanged.
4. **Artifact** `research/sprint_004/19_TSMOM_OOS_VALIDATION.json`:
   - protocol description, fold dates, per-config test-fold metrics (net return, max weight, gross, max |net|, meets_risk, live_gate_satisfied)
   - `live_eligible: false` globally and per row
   - `oos_supports_live_path: false` unless **test-fold** (not train) gate is true for a config **and** note still says separate LIVE ticket required
5. **Model identity:** Use distinct research factor_ids; do not invent LIVE promotion. If paper loop requires an artifact id, document reuse vs new id; do not claim PAPER_APPROVED for new configs without a promotion ticket.
6. **Tests:** suite green; new helpers only if pure/small.

## Out of Scope

- LIVE promotion  
- Expanding the lookback/skip grid on the test fold  
- Changing risk limits  
- Mutating historical artifacts `08`–`17` (append-only new files)

## Acceptance (Jr)

1. `.venv/bin/python -m pytest tests/execution/ tests/acquisition/ -q --tb=short`
2. `.venv/bin/python -m ruff check src/cryptofactors/execution scripts/`
3. `19_TSMOM_OOS_VALIDATION.json` present; all `live_eligible: false`
4. `python3 scripts/check_repo_control.py`

## Stop Condition

After Sr: AWAITING_REVIEW, Next ticket authorized: NONE.
