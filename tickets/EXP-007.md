# EXP-007 — Full-Window Paper Screen of Remaining Multi-Fold Candidates

**Priority:** P0  
**Status:** ACCEPTED  
**Dependencies:** PAPER-007 (ACCEPTED), EXP-006, DATA-004, ALLOC-001  
**Layer:** research / paper evidence  
**Architecture:** same as PAPER-007. **No LIVE.**

## Objective

`tsmom_14_0` full-window paper is **−0.63%** (gate false) despite 2/3 multi-fold gates. Screen the other EXP-006 configs with **2/3 fold gates** on the **same continuous full window** before pivoting the factor family.

## Scope

1. **Configs (frozen):** `tsmom_14_3`, `tsmom_60_0` (required). Optional: baseline `tsmom_30_7` for reference.
2. **Protocol:** Same as PAPER-007 — weekly decisions, 2024-04-01 → 2026-07-23 (or document), DATA-004 dataset pin, ALLOC-001 risk, real_asof.
3. **Identity:** Distinct research `model_artifact_id` per config (e.g. `mod_tsmom_14_3_v1`, `mod_tsmom_60_0_v1`); `effective_time` = first decision.
4. **Artifact** `research/sprint_004/23_TSMOM_FULLWINDOW_SCREEN.json`:
   - per config: net return, max \|w\|, gross, max \|net\|, meets_risk, is_complete, live_gate_satisfied, live_eligible: false
   - ranking; `any_fullwindow_gate: true/false`
   - cross-ref PAPER-007 / EXP-006
5. **Do not mutate** 08–22.
6. **Tests:** suite green.

## Out of Scope

- LIVE  
- Re-optimizing lookback/skip  
- Changing risk limits  
- BAR-001 quality fix  

## Acceptance (Jr)

1. `.venv/bin/python -m pytest tests/execution/ tests/acquisition/ -q --tb=short`
2. `.venv/bin/python -m ruff check src/cryptofactors/execution scripts/`
3. `23_TSMOM_FULLWINDOW_SCREEN.json` present; all `live_eligible: false`
4. `python3 scripts/check_repo_control.py`

## Stop Condition

After Sr: AWAITING_REVIEW, Next ticket authorized: NONE.
