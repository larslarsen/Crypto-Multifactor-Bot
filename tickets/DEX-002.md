# DEX-002 — Multi-Provider Free DEX OHLCV Fan-Out (Post DATA-007)

**Priority:** P0  
**Status:** ACCEPTED
**Next:** NONE  
**Dependencies:** DATA-007 (ACCEPTED)  
**Layer:** ingest / catalog  
**Architecture:** sharded providers; content-addressed publish. **No Birdeye OHLCV. No LIVE.**

## Objective

Implement production ingestion that **spreads DEX OHLCV/pool stats across multiple free providers** from DATA-007’s `recommended_fanout`, with per-provider rate limiters and unified canonical (or multi-source) pool bars for a **screened** pool set—not the entire chain.

## Scope (summary)

1. Provider interface + token-bucket / min-interval per source (from matrix).
2. Shard work by `(provider, chain, pool)` with watermarks; idempotent publish.
3. Merge/dedupe policy: prefer primary provider; secondary for gap-fill; provenance on every row.
4. Screening gate before enqueue: only pools/tokens passing liquidity/volume criteria (config).
5. DEX death: mark inactive when liquidity/volume below threshold for N days (pragmatic delist); feed universe as-of.
6. Artifact: capacity + coverage report after first controlled backfill window.

## Hard constraints

- Birdeye key never used for bars.
- Free tiers only unless a later ticket authorizes paid.
- Fail closed on quality; no silent gap fill.

## Stop Condition

After Sr: AWAITING_REVIEW, Next NONE.
