# EXP-004 — TSMOM Lookback Grid under Neutrality-Preserving Risk

**Priority:** P0  
**Status:** AWAITING_REVIEW  
**Dependencies:** ALLOC-001 (ACCEPTED), EXP-003 (ACCEPTED), DATA-003  
**Layer:** research / factors / paper evidence  
**Architecture:** reuse ALLOC-001 `enforce_risk_limits`, real as-of path. **No LIVE. No new risk limits.**

## Objective

Under neutrality-preserving 0.15/1.0 enforcement, MOM-TS-01 (`30_7`) is **−6.52%** over ~12m real data (unconstrained −5.28%). Search whether **any** standard TSMOM (lookback, skip) configuration is risk-compliant **and** profitable on the same real window before abandoning or redesigning the family.

## Scope

1. **Grid** (minimum): lookbacks ∈ {7, 14, 30, 60, 90} days; skip ∈ {0, 3, 7} days (or document reduced grid if runtime-bound). Use existing `TimeSeriesMomentumFactor` / makers where possible.
2. **Each cell:** risk-enforced paper session on same universe + real bars window as EXP-003/ALLOC-001 (or document if data missing).
3. **Artifact** `research/sprint_004/18_TSMOM_GRID_RESULTS.json`:
   - per config: net return, max weight, gross, max |net|, meets_risk, live_gate_satisfied, live_eligible: false
   - ranking table; best config highlighted
4. **Decision note** in artifact: `recommend_live_path: false` unless some config has live_gate_satisfied true (still no LIVE ticket from this work).
5. **Tests:** only if new pure helpers; otherwise suite green.

## Out of Scope

- LIVE promotion  
- Changing 0.15 / 1.0  
- New factor families beyond TSMOM lookback/skip  
- Funding/perp mechanics changes  

## Acceptance (Jr)

1. `.venv/bin/python -m pytest tests/execution/ tests/acquisition/ -q --tb=short`
2. `.venv/bin/python -m ruff check src/cryptofactors/execution scripts/`
3. `18_TSMOM_GRID_RESULTS.json` present; all rows `live_eligible: false`
4. `python3 scripts/check_repo_control.py`

## Stop Condition

After Sr: AWAITING_REVIEW, Next ticket authorized: NONE.
