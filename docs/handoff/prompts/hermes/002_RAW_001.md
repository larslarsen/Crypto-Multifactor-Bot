# Hermes prompt — RAW-001

Implement only `tickets/RAW-001.md` after CAT-001 has been accepted. Preserve exact source bytes. Stream writes, hash during write, publish atomically, and make duplicate retries idempotent. Do not parse market data, create canonical bars, or add an HTTP framework. Include corruption and interrupted-write tests. Stop after the ticket.
